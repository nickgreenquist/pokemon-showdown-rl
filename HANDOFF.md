# Handoff — 2026-08-23 (evening)

Session closed at a decision boundary; nothing mid-flight, box idle,
server healthy on :8000, tree clean and PUSHED through 29c133e, all
results backed up. Read STATUS.md — headline: **CH3 R3 COMPLETE,
verdict VALUE-LIMITED (E3 LOO-ensemble evaluator +0.036 over E0 at
screen grade); both off-SH anchors fired P2 (R2's search credit is
SH-facing, caveat executed in README)**. Then the six 08-23
SESSION_LOGS entries for full numbers.

MAINTAINER DECISIONS ALREADY GIVEN (this session, act on them):
(a) **R4 ensemble-critic pre-reg: APPROVED to design** — run the
    2-Opus design cycle (design-decisions-two-opus memory) for a
    LETTER-BEARING credit test: depth-1 search@M with an ensemble
    critic evaluator (pure, zero training; E3 screen +0.036
    directional). Fresh pre-reg + ratification required (MF-7); the
    ANCHOR BATTERY is mandatory (CLAUDE.md convention: SH locked +
    clone 500 + FP@100 250) — after P2, off-SH transfer is THE
    question. Runtime ~3h 4-wide once ratified.
(b) **MC-leaf/λ-blend: DROPPED** ("agree with both") — record as a
    ruling in the log when convenient; do not build the State→obs
    bridge.
Also queued: oracle-team diag (BARRED binary, built, unrun, 35 min);
E2(σ=0.2) upgrade stays maintainer-buyable.

Landmines fresh from this session: bash 3.2 (no ${var,,} in runners);
never quote per-arm numbers from unsorted multi-file greps — read
named files; FP-side runners need the auto-relaunch + crash-forfeit
read rule (poke-engine gen1 5th-move panic).

Fold this back to the empty stub on pickup.
