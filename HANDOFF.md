# Handoff — 2026-08-23

Nothing mid-flight; the session closed at a verdict boundary. Read
STATUS.md — headline: **R2 B1 CREDIT, search wins, new best 0.7928
(+0.0693 over greedy, 4/4 lanes, zero F-gates)**. Then the two newest
SESSION_LOGS.md entries (the 08-22 'latest' launch-sequence entry and
the 08-22/23 overnight readout) for the full numbers and the
pre-registered B1 consequences.

Next actions, in order (all pre-registered, STATUS item 1):
(a) SH-exploitation falsifier — two-orientation h2h vs the BC clone
    (D26 H4 machinery, ~20 min);
(b) h2h vs Foul Play (purity-legal anchor, 0.8307* vs SH);
(c) R3 mechanism grid — design §4 R3 (NON-CREDITING; E1-E4 dials);
    transcribe to an executable pre-reg like ch3_rung2.yaml first;
(d) D7(a) ladder contradiction -> maintainer ruling (named, not acted).

Tree green (suite 428/17), pushed through d28dce5, server healthy on
:8000 (restart: cd showdown && node pokemon-showdown start --no-security;
config simulator:4 must be set). results/ch3_r2 backed up. Fold this
back to the empty stub on pickup.
