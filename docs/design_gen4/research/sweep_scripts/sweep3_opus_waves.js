export const meta = {
  name: 'gen4-research-sweep-opus',
  description: 'Gen4 design research sweep (Opus-only, in waves): Wang, H&L, poke-env source, vendored Showdown gen4 data, our landed EncoderSpec seam, anchors; reconcile; follow-ups',
  phases: [
    { title: 'Wave 1', detail: 'encoder-critical sources: our seam, gen4 pool, poke-env state + env layer, gen4 mechanics', model: 'opus' },
    { title: 'Wave 2', detail: 'Wang thesis + poke-env fork, abilities/items, project record, H&L/metagrok', model: 'opus' },
    { title: 'Wave 3', detail: 'Wang showdown fork, ps-ppo/Metamon obs, foul-play/pokejax, mechanics literature', model: 'opus' },
    { title: 'Reconcile', detail: 'index all notes, find conflicts and gaps, name follow-ups', model: 'opus' },
    { title: 'Follow-up', detail: 'targeted agents for the gaps the critic named', model: 'opus' },
  ],
}

const MAIN = '/Users/nickgreenquist/Documents/Projects/pokemon-showdown-rl'
const WT = '/Users/nickgreenquist/Documents/Projects/pokemon-showdown-rl-gen4design'
const PE = '/opt/anaconda3/envs/pokemon-showdown-rl/lib/python3.13/site-packages/poke_env'
const SD = MAIN + '/showdown'
const PW = MAIN + '/docs/prior_work'
const PSPPO = '/Users/nickgreenquist/Documents/Projects/ps-ppo'
const FP = '/Users/nickgreenquist/Documents/Projects/foul-play'
const MG = '/Users/nickgreenquist/Documents/Projects/metagrok'
const SCRATCH = '/private/tmp/claude-501/-Users-nickgreenquist-Documents-Projects-pokemon-showdown-rl/b1478b5b-c556-4e2e-9100-b0db7e234069/scratchpad'
const RD = SCRATCH + '/research'
const SNAP = SCRATCH + '/main_snapshot'
const OPTS = { model: 'opus', effort: 'high' }

const PREAMBLE = `You are one research agent in a staged, docs-only design sweep for the GEN 4 chapter (format gen4randombattle) of an RL project that today plays Pokemon Showdown gen1randombattle through poke-env 0.15.0 with a hand-built 828-dim encoder. Two things are running on this machine that you must not disturb: a LIVE RATED LADDER RUN (200 games vs humans on play.pokemonshowdown.com, driven by another session from the main tree, BLIND until it ends) and nothing else heavy may run beside it.

HARD BARS (violating any one is a session-ending failure):
- READ-ONLY everywhere except SCRATCH (${SCRATCH}). Never edit, create, checkout, stash, rebase, or commit anything inside any git tree (MAIN, WT, PSPPO, FP, MG). Never run pip install or conda install. The local Showdown server is STOPPED for the ladder run: never start it, never touch port 8000, never start a battle, never construct a poke-env Player/Env/PSClient object, never run rl.train, pytest, or any benchmark. Never kill or signal any process. Never read MAIN/runs, MAIN/logs, or MAIN/results/ladder. Never fetch pokemonshowdown.com user profiles, ladder boards, or replays (the run is blind); GitHub source and Bulbapedia/Smogon pages are fine, reddit is unfetchable.
- CPU politeness: grep/sed/cat/jq/head/wc are fine. If you need Python to parse data, run 'nice -n 19 /opt/anaconda3/envs/pokemon-showdown-rl/bin/python' on a short script that touches no network; importing poke_env for data tables (GenData) is allowed, instantiating players/envs is not. 'node -e' for pure data parsing under nice -n 19 is allowed but prefer grep/jq. Do not run pdftotext on the Metamon PDF (14 MB): its text already exists (below).
- The shell is zsh: quote glob patterns, e.g. grep -rn --include='*.py'.
- Token discipline: read with grep -n then sed -n ranges rather than dumping whole files; do not paste entire source files into your note; quote the lines that carry the fact.

PATHS (all read-only):
- SNAP = ${SNAP}: a read-only snapshot of the repo's tracked tree at main @2738025 (2026-09-04, AFTER the audit-fixes merge and the 100M readout). THIS IS THE AUTHORITY for repo code and docs; cite repo paths relative to the repo root and say '(main@2738025)'. The per-gen encoder seam HAS LANDED: SNAP/rl/envs/encoder_spec.py (frozen EncoderSpec, GEN1, spec_for_format refusing other gens with a work list); its landing record is SNAP/docs/archive/AUDIT_BRANCH_LOG.md section 'F-08' (around lines 303-345) and SNAP/docs/archive/AUDIT_ACTION_PLAN.md F-08; the adjacent unruled proposal is SNAP/docs/proposals/F07_encoder_config_block.md. SNAP/RESULTS.md §18 is the 100M readout (cell P3).
- WT = ${WT}: our design worktree (branch gen4-design, still based at 60c1225; the final docs will be written there by other agents later — you do not write there).
- MAIN = ${MAIN}: the live main tree; use it ONLY for the gitignored, un-snapshotted material: MAIN/showdown (vendored pokemon-showdown 0.11.11 @ 59da482: data/, data/mods/gen4/, data/mods/gen1/, data/random-battles/gen4/{sets.json,teams.ts}, data/random-battles/gen5/teams.ts, sim/, config/formats.ts), MAIN/docs/prior_work big files, and MAIN/results/design_ch5_100m/ (the 2-Opus synthesis precedent). Nothing else in MAIN.
- SD = ${SD}; PW = ${PW} (wang_fork_diffs.md; wang2024_mit_thesis_randbats_rl.pdf; huang_lee_2019_selfplay_pokemon.pdf; grigsby2025_metamon.pdf; pokejax/; HUANG_LEE_DEEP_READ.md; README.md = the VERIFIED prior-work index — read its entry for any system BEFORE citing that system; several popular claims are recorded there as wrong).
- PE = ${PE} (installed poke-env 0.15.0 source; data/static/{moves,pokedex,typechart}/gen4*.json).
- PSPPO = ${PSPPO} (ps-ppo clone; a copy of its 1b13ae0-era obs files sits in ${RD}/psppo_1b13ae0), FP = ${FP} (foul-play clone with our gen1 patch applied), MG = ${MG} (Huang & Lee's metagrok clone).
- Already-converted PDF text (reuse; do not re-run pdftotext): ${RD}/_wang_thesis_paged.txt and ${RD}/_wang_thesis.txt (Wang), ${RD}/_huanglee_paged.txt (H&L), ${RD}/_metamon_paged.txt (Metamon). Page images of Wang p.26/29/31-32 exist as ${RD}/_wang_p*.png and ${RD}/_wang_fig41_crop-29.png if a figure must be read. Two earlier helper dumps exist (${RD}/_gen4_dexdump.json from ${RD}/gen4dump.js; ${RD}/_gen4pool_probe.out.json from ${RD}/_gen4pool_probe.js): reuse only after reading the script that produced them, and cite the script.

VERIFICATION STATUS VOCABULARY (tag EVERY finding with exactly one):
- tree-verified: checked against a file in the repo tree (SNAP: rl/, scripts/, configs/, tests/, docs/) or the vendored SD data/sim, i.e. the game as we actually run it.
- source-verified: checked against an external primary source on disk: PE source, Wang's diffs or thesis text, the H&L text or MG code, PSPPO, FP, Metamon text.
- literature-only: from a secondary write-up, a web page, memory, or the prior-work index without re-checking the primary.
- needs-live-verification: only a running server or battle can confirm it; BARRED until the ladder run and any later fleet complete; say exactly what the check would be.

OUTPUT CONTRACT:
1. Write a complete, self-contained markdown research note to ${RD}/<note-name>.md (the note name is given in your task). Header: agent name, date 2026-09-04, every source you read with path and line ranges or pages, and the status legend above. Body: findings grouped by topic; each finding a paragraph or table row carrying its status tag and a precise citation (file:line, or PDF page/table/figure). Quote verbatim for load-bearing numbers and formulas. Include, where relevant, sections 'gen1 encoder assumptions this breaks', 'open questions for the maintainer', and 'unread / unverified'. Be thorough; never invent a citation; if you did not read it, say so.
2. Return the structured summary (schema enforced): note_path, summary (<= 350 words), key_findings (the 10-25 load-bearing ones, each with status + citation), gen1_assumptions_broken, open_questions, unverified.

Downstream docs written from the notes: mechanics_delta.md (gen1->gen4 rules delta that matters to an encoder and a policy), pokeenv_gen4_survey.md (what pinned poke-env supports for gen4randombattle), encoder_requirements.md (the gen4 EncoderSpec designed against the LANDED seam), anchors_and_eval.md (what SimpleHeuristicsPlayer is worth in gen4, the most-damage-typed anchor, a Foul-Play-equivalent for gen4, how the gen1 anchor-battery convention translates), open_questions.md (maintainer rulings with recommendation + losing argument), search_depreciation.md (stretch). Record cross-references for the other docs, but stay inside your own source family.

YOUR TASK:
`

const RESEARCH_SCHEMA = {
  type: 'object',
  required: ['note_path', 'summary', 'key_findings', 'gen1_assumptions_broken', 'open_questions', 'unverified'],
  properties: {
    note_path: { type: 'string' },
    summary: { type: 'string' },
    key_findings: {
      type: 'array',
      items: {
        type: 'object',
        required: ['claim', 'status', 'citation'],
        properties: {
          claim: { type: 'string' },
          status: { type: 'string', enum: ['tree-verified', 'source-verified', 'literature-only', 'needs-live-verification'] },
          citation: { type: 'string' },
        },
      },
    },
    gen1_assumptions_broken: { type: 'array', items: { type: 'string' } },
    open_questions: { type: 'array', items: { type: 'string' } },
    unverified: { type: 'array', items: { type: 'string' } },
  },
}

const T = {
  our_encoder_seam_inventory: `Inventory what the LANDED per-gen seam already provides and every gen-1 assumption that still lives outside it, then propose the gen-4 additions AGAINST that interface. Read in full: ${SNAP}/rl/envs/encoder_spec.py (286 lines: the frozen EncoderSpec, GEN1, spec_for_format, the gen-4 work list in the class docstring — quote that list verbatim), ${SNAP}/rl/envs/showdown.py (1366 lines: how the spec is threaded through _fill_mon/_fill_active/_fill_move/embed_battle/_fill_ids/_species_id/_move_id/_move_slots_aliased; where block strides and OBS_DIM are derived from GEN1 plus process flags; why embed_battle refuses a non-GEN1 spec; the v2 effect block _effect_block/_move_obj building Move(id, gen=1); the set prior path _opponent_move_slots; _spe_est/_speed_edge; hl_event_sum; calc_reward; battle_outcome; the SPECIAL_MOVES aliasing and mask-desync recovery; PoolPlayer's own encode path), ${SNAP}/tests/test_encoder_spec.py (467 lines: what the tape hash gate pins — which flag combos, which hashes, which env vars; how the goldens were made), ${SNAP}/rl/envs/randbats_prior.py and the structure of ${SNAP}/rl/envs/data/gen1_randbats_sets.json, ${SNAP}/rl/networks/entity_deepsets.py (tokenizer, species_vocab/move_vocab origins, pointer head over 6+4, N_ACTIONS import), ${SNAP}/rl/networks/opp_action.py (L6 over 4 move slots), ${SNAP}/rl/networks/mlp.py, ${SNAP}/rl/train.py (fake_spaces by format, ENCODER_FINGERPRINT stamping), ${SNAP}/scripts/eval_checkpoint.py (the cross-encoder shim, obs_dim override), ${SNAP}/rl/search/{shadow_battle.py,bridge.py,determinize.py,harvest.py,expansion.py} (gen1 assumptions in search), ${SNAP}/rl/envs/showdown_async.py, ${SNAP}/rl/envs/make.py. Then grep SNAP/rl, scripts, configs, tests for gen1, GEN1, gen1randombattle, Discrete(10), 'fight', ID_SCALE, from_gen, type_chart (--include='*.py' --include='*.yaml' --include='*.sh'). Read the F-08 landing record ${SNAP}/docs/archive/AUDIT_BRANCH_LOG.md (the F-08 section and its 'Open questions for the maintainer'), ${SNAP}/docs/archive/AUDIT_ACTION_PLAN.md F-07/F-08/F-11/F-20, and ${SNAP}/docs/proposals/F07_encoder_config_block.md (its §5 interaction with the spec, §7 rulings wanted). Deliver: (1) a table of what the seam ALREADY parameterizes (each EncoderSpec field and derived property, with the showdown.py line that reads it) and what it explicitly refuses (the NotImplementedError work list); (2) a table of every gen-1 assumption still OUTSIDE the spec: file:line | what it assumes | gen4 status (breaks / survives / needs a per-gen table) | proposed owner (a new spec field, a new block, a per-format data file, or a process flag) — cover at least: block strides and OBS_DIM derivation, the v2 effect block and its gen-1 Move data and move-volatile table, the set prior and _opponent_move_slots, ID_SCALE 256 with species 1..493 and moves 1..467 at gen 4 (does id/256 still fit in Box(-1,4) and stay exact in float32? compute), the tokenizer vocab sizes and embedding tables, opp_action's per-move-slot head, privileged_block, hl_event_sum's event parsing, _spe_est/_speed_edge stat formula, the physical flag (already move.category — confirm at the line), MUST_RECHARGE and preparing, status_counter semantics, the mask-desync recovery and SPECIAL_MOVES for gen4 (struggle/recharge only), the search modules, and every remaining Discrete(10) literal in scripts; (3) the exact test contract a gen-4 spec must keep green (test names, hashes, flag combos) and the tests a gen-4 spec would need to add; (4) a proposed gen-4 EncoderSpec extension, grounded in the landed dataclass: new fields (side_conditions, weathers, fields, item/ability vocab tables or effect-class tables, per-spec species/move num ranges, a format-keyed set-prior loader, an effect-block builder keyed by gen, per-spec strides and obs_dim, a fingerprint), which showdown.py sites consume each, and how the spec would be selected (spec_for_format vs the F-07 config block). Flag everything as a PROPOSAL to reconcile with the maintainer. Note name: our_encoder_seam_inventory.md`,

  showdown_gen4_pool: `Characterize the gen4 random-battle team pool as vendored: ${SD}/data/random-battles/gen4/sets.json and ${SD}/data/random-battles/gen4/teams.ts (it extends ../gen5/teams — read ${SD}/data/random-battles/gen5/teams.ts and any further base it inherits for item assignment, EVs / IVs / nature, Hidden Power, level scaling, team constraints), the ${SD}/config/formats.ts entry for gen4randombattle (ruleset; confirm whether Team Preview is absent), ${SD}/data/rulesets.ts for the named rules, and ${SD}/data/mods/gen4/formats-data.ts if the generator reads it. Deliver exact counts with the commands used (jq or a nice'd python one-off): number of species keys in sets.json; number of distinct moves across all movepools; number of distinct abilities; the set of items the generator can assign (enumerate every item literal in teams.ts and the inherited base with the condition under which it is assigned; mark which are gen4-legal); level range and distribution; the roles list; sets per species; whether movepools are exact 4-move sets or pools that are sampled (and the sampling constraints: MOVE_PAIRS, NO_STAB, HAZARDS, SETUP, priority rules); how the ability is chosen when a set lists several; the EV / IV / nature policy and the Hidden Power IV rule; team-level constraints (type caps, weakness caps, teamDetails counters for hazards, Rapid Spin, weather, screens); the rules applying to randbats from the format entry (Sleep Clause? Endless Battle Clause? HP Percentage Mod, Cancel Mod). Compare against the gen1 pool (${SD}/data/random-battles/gen1/; the index says ~146 species) for scale. Then derive VOCAB SIZES for an encoder: species (pool plus formes that can appear mid-battle: Rotom formes, Giratina-Origin, Castform, Cherrim, Arceus plates, Wormadam, Shaymin-Sky — check what the pool contains), moves (pool moves plus Struggle plus Hidden Power variants plus moves a mon can end up using mid-battle: Metronome, Sleep Talk, Mimic, Transform / Ditto, Assist, Copycat — say whether pool = universe), abilities, items. Our gen1 encoder emits ids as id/256 in float32 and the landed seam records species 1..493 / moves 1..467 for gen 4 (${SNAP}/rl/envs/encoder_spec.py docstring): flag every vocab that exceeds 255 and what a poke-env 'num' (pokedex number / move num) based id would give versus a pool-local id. Note name: showdown_gen4_pool.md`,

  pokeenv_battle_state: `Survey the pinned poke-env 0.15.0 battle-state model for gen4 under ${PE}: battle/abstract_battle.py, battle/battle.py, battle/pokemon.py, battle/move.py, battle/effect.py, battle/side_condition.py, battle/weather.py, battle/field.py, battle/status.py, battle/move_category.py, battle/target.py, data/gen_data.py, data/normalize.py, and the static data data/static/typechart/gen4typechart.json, data/static/pokedex/gen4pokedex.json, data/static/moves/gen4moves.json. Report with file:line: (1) Pokemon fields and their gen4 semantics: item (values for unknown / none / known, how revealed, Trick / Knock Off / consumed berries), ability and possible_abilities (how set from the pokedex, how revealed, Trace), stats / base_stats (spd separate; how opponent stats are estimated), boosts (7 keys), status and status_counter (sleep and toxic counters: how maintained from protocol messages — this decides whether an encoder can carry a sleep-turn feature), effects (the Effect enum: which gen4 volatiles map to a member and which fall to UNKNOWN; count; check SUBSTITUTE, PROTECT, ENCORE, TAUNT, TORMENT, LEECH_SEED, CONFUSION, CURSE, PERISH_SONG, YAWN, ATTRACT, INGRAIN, AQUA_RING, MAGNET_RISE, TRAPPED / partial trap, DISABLE, EMBARGO, HEAL_BLOCK, FOCUS_ENERGY, SLOW_START, FLASH_FIRE, DESTINY_BOND, MUST_RECHARGE, LIGHT_SCREEN / REFLECT as side conditions), must_recharge, preparing, protect_counter, first_turn, revealed, moves dict (Move objects; current_pp / max_pp: who tracks PP, decremented locally or parsed), level, gender, the dynamax / tera guards; (2) Battle fields: weather (dict Weather -> turn; how gen4 permanent ability weather appears), fields (Field enum: TRICK_ROOM, GRAVITY, ...), side_conditions / opponent_side_conditions (SideCondition enum incl. STEALTH_ROCK, SPIKES, TOXIC_SPIKES, REFLECT, LIGHT_SCREEN, SAFEGUARD, MIST, TAILWIND, LUCKY_CHANT; how layers are counted), turn, force_switch / _force_switch, maybe_trapped / trapped, can_mega / can_z / can_dynamax / can_tera (inert in gen4), available_moves / available_switches, team_preview handling (gen5+ only: confirm a gen4 randbats battle never gets it), how the opponent's team is revealed and stored, last-move / history data (_replay_data or any event log an encoder could use); (3) Move fields from gen4moves.json: category (PHYSICAL / SPECIAL / STATUS per move), base_power, accuracy, priority, pp, secondary, boosts, self_boost, heal, drain, recoil, self_switch, force_switch, volatile_status, side_condition, weather, status, flags, target, is_protect_move, breaks_protect, expected_hits, crit_ratio, sleep_usable, thaws_target, and the SPECIAL_MOVES aliasing (struggle / recharge; gen1's 'fight' placeholder is gen1-only); (4) the gen4 type chart from gen4typechart.json: exactly 17 types (no Fairy; Dark and Steel present), multiplier semantics and GenData.from_gen(4).type_chart; (5) damage-calc modules present (calc/damage_calc_gen1_2.py, calc/damage_calc_gen9.py: nothing for gen4? implications for an engineered damage feature); (6) TODO / FIXME / known-gap comments, and parsing paths where a gen4 protocol message would land in an unknown bucket. For every behaviour that depends on live protocol traffic (what the server actually sends for gen4 items, abilities, weather, hazards), tag needs-live-verification and specify the exact post-ladder check. Note name: pokeenv_battle_state.md`,

  pokeenv_env_layer: `Survey the pinned poke-env 0.15.0 environment / action / player layer for gen4randombattle under ${PE}: environment/singles_env.py, environment/env.py, environment/single_agent_wrapper.py, player/player.py, player/battle_order.py, player/baselines.py (read it in FULL), player/utils.py, ps_client/ps_client.py (format and team handling for a team-less format, the timer command), data/gen_data.py. Report with file:line: (1) the action space: get_action_space_size(gen=4) — verify 10 and give the exact index layout (which indices are switches and which are moves; the ordering of switch slots vs battle.team order; how order_to_action / action_to_action map for gen4 with no mega / z / dynamax / tera branches; how the action mask is built for gen4 and how trapped, force_switch, must_recharge, disabled moves, and Struggle show up in it); (2) how the env handles a gen4 battle start (no team preview), request parsing, and the wrapper's obs / reward hooks, plus the max_concurrent_battles literals and the start_timer_on_battle_start knob (our landmine: every seat sends /timer on); (3) SimpleHeuristicsPlayer end to end: every branch and every gen-gated condition — the ENTRY_HAZARDS dict and how it is used, _estimate_matchup (type chart via GenData.from_gen(battle.gen)), _should_switch_out, _stat_estimation (describe the +1-boost bug the index mentions ps-ppo patched, exactly), the speed-tier logic, the setup-move logic (the index calls SH's setup branch 'dead': find out why), dynamax / tera guards (inert for gen4), the move-scoring formula (base power, type multiplier, STAB, accuracy, expected hits, physical/special handling — does it read move.category or infer from type?), MaxBasePowerPlayer and RandomPlayer. Conclude concretely which SH branches are INERT in gen1 and LIVE in gen4 (JOURNEY's argument that SH strength drifts across generations), and whether anything makes SH relatively weaker in gen4 (ignores items / abilities / weather / hazard damage on switch-in?); (4) anything else in this layer that is gen-specific and would differ between gen1 and gen4 (SPECIAL_MOVES aliasing, the 'fight' placeholder, struggle, recharge). Tag live-protocol-dependent claims needs-live-verification with the exact post-ladder check. Note name: pokeenv_env_layer.md`,

  showdown_gen4_mechanics: `Catalogue gen4 battle mechanics AS IMPLEMENTED in the vendored Showdown, focusing on what differs from gen1 and what an encoder or policy must represent. Sources: ${SD}/data/mods/gen4/{scripts.ts,conditions.ts,moves.ts,rulesets.ts,pokedex.ts} (overrides), the base ${SD}/data/{conditions.ts,scripts.ts,moves.ts,typechart.ts,rulesets.ts} (base = current-gen semantics; a gen4 battle uses base data unless the gen4 mod or an intermediate mod in its inherit chain overrides it: READ the gen4 mod's 'inherit' field in scripts.ts and follow the chain so that you attribute each mechanic to the right file), and ${SD}/data/mods/gen1/{scripts.ts,conditions.ts,moves.ts,typechart.ts} for the gen1 side of each delta; ${SD}/sim/battle-actions.ts and sim/pokemon.ts for damage, crit and turn-order code when needed. Cover each with file:line and a status tag: damage formula and rounding; critical hits (rate, stages, multiplier; gen1 speed-based vs gen4 stage-based; high-crit moves; Focus Energy); physical/special per-move category (the 'category' field in base moves.ts; where the gen1-3 by-type rule lives — check the gen3 mod / gen4 scripts for getCategory-style overrides); the type chart (17 types; every cell that differs from gen1: Ghost vs Psychic, Bug vs Poison, Poison vs Bug, Ice vs Fire, the Dark and Steel rows and columns); STAB; accuracy / evasion; status conditions (sleep: turn-count range in gen4 vs gen1, whether the mon can act on the wake turn, Sleep Talk / Rest interaction; freeze: thaw chance; paralysis: speed multiplier and full-para rate; burn; poison; toxic counter and whether it resets on switch; confusion turns and self-hit rate; attract); volatiles and multi-turn state (Substitute HP and behaviour, Protect / Detect success counter, Encore, Taunt, Torment, Disable, Leech Seed, Curse, Perish Song, Yawn, Ingrain, Aqua Ring, Magnet Rise, partial trapping, Mean Look / Block / Spider Web, Bide, Rage, Uproar, Rollout / Ice Ball, Outrage / Thrash / Petal Dance lock plus confusion, two-turn moves (Fly / Dig / Dive / Bounce / Shadow Force / Solar Beam / Skull Bash / Sky Attack), recharge moves (Hyper Beam / Giga Impact), Charge, Stockpile, Destiny Bond, Grudge, Embargo, Heal Block, Gastro Acid, Power Trick, Lock-On, Foresight / Miracle Eye / Odor Sleuth, Nightmare, Minimize / Defense Curl flags, Roost typing, Wish, Future Sight / Doom Desire, Baton Pass transfer set, U-turn self-switch timing, Pursuit on a switch, Fake Out first-turn, Sucker Punch, Counter / Mirror Coat / Metal Burst, Endeavor, Trick / Switcheroo, Knock Off, Rapid Spin, Roar / Whirlwind, Haze, Explosion / Selfdestruct Defense-halving in gen4, Struggle recoil, Hidden Power type and power derivation); side conditions (Stealth Rock damage by type effectiveness, Spikes layers and damage, Toxic Spikes layers / effects / Poison-type absorption, Reflect / Light Screen turns and Light Clay, Safeguard, Mist, Tailwind, Lucky Chant); weather (rain / sun / sand / hail effects, durations 5 vs 8 with the rocks, whether ability-set weather is INDEFINITE in gen4, sand / hail chip and the immune types, Sand Veil / Snow Cloak); fields (Trick Room, Gravity); priority brackets and speed ties; PP and Pressure; the Endless Battle Clause and the turn cap; how the Sleep Clause Mod, Freeze Clause, OHKO Clause, Evasion Clause apply to randbats. Also state each gen1 quirk that DISAPPEARS (the 1/256 miss, partial trapping skipping the victim's turn, Hyper Beam no-recharge on KO, Counter / Bide / Substitute oddities, the burn / para stat-drop reset bug, Wrap, Toxic plus Leech Seed stacking, sleep 1-7 turns with no move on the wake turn, freeze permanence, speed-based crits, the single Special stat, the Focus Energy bug). Produce the delta as a table: mechanic | gen1 (file:line) | gen4 (file:line) | what an encoder must carry | what a policy must reason about. Do not cover abilities and items in depth (another agent does) beyond naming the mechanics they hook. Note name: showdown_gen4_mechanics.md`,

  wang_thesis: `Read Wang 2024 (Jett Wang, MIT MEng thesis, 'Winning at Pokemon Random Battles Using Reinforcement Learning') in FULL from the existing text ${RD}/_wang_thesis_paged.txt (page-separated; the PDF is ${PW}/wang2024_mit_thesis_randbats_rl.pdf if you need a figure: page images ${RD}/_wang_p26-26.png, _wang_p29-29.png, _wang_p31-31.png, _wang_p31-32.png, _wang_fig41_crop-29.png exist — view them with the Read tool). Read the index entry for it in ${PW}/README.md first (grep -n 'wang2024'). Extract with page/table/figure citations and verbatim numbers: (a) the format and environment (gen4randombattle; the 3v3 surrogate used for Bayesian hyperparameter optimization; any Showdown fork used during training vs only at test time); (b) the observation design in full, Tables A.1/A.2 and the surrounding text: every feature, its encoding (one-hot / bins / scalars), dims, how items, abilities, weather, hazards, volatiles, effect durations, PP and HP are represented (the index records HP binned, PP as floor(pp^(1/3)), multi-turn effect durations as one-hot counters to restore Markovianity), the total observation size, and how unrevealed opponent information is represented; (c) the action space (size; positional or identity-based; the index calls it a '494-way identity space', verify) and masking; (d) the reward; (e) EVERY hyperparameter in Table A.3 verbatim plus the LR schedule formula (the index quotes 10^-4.23/(8x+1)^1.5) and the LR-anneal ablation (§3.1.4: constant 0.55 -> annealed 0.80). RESOLVE A DISCREPANCY explicitly: the session brief says 'gamma=1.0/lambda=0.95 at 128k steps/update' for Wang; JOURNEY.md (${SNAP}/JOURNEY.md step 4) says 'gamma 0.9999, lambda 0.754, 7 epochs, clip 0.0829, value clip 0.0184, ent 0.0588, vf 0.4375, grad-norm 0.543, n_steps 78x512, batch 1024, hidden 256, features 896'; docs/IDEAS_POST_100M.md says Wang lambda 0.754 — report exactly what the thesis says, where, and which document is wrong, including steps per update (n_steps x n_envs) and total training steps; (f) training scale (total steps, wall clock, hardware, number of envs), SB3 usage, number of seeds and error bars if any; (g) results: Table 4.1 (0.786 vs SimpleHeuristicsPlayer) vs Figure 4.1 (read as ~0.85; the index flags this as unreconciled — try to reconcile from the text: different checkpoint? n? sampling vs greedy? 3v3 vs 6v6?), the ~0.575 at 6M steps, and the ladder result's exact numbers (rank, Elo, Glicko, GXE, n, whether the laddered agent used MCTS); (h) MCTS at test time: simulations / time budget, determinization of hidden information (how opponent sets are sampled; constrained team regeneration), how policy / value are used, the reported gain of search over the raw network vs SH and on the ladder; (i) negative results: curriculum (§5.1.3), the bootstrapping / transfer variant (JOURNEY: 'no significant improvement'), anything else; (j) what the thesis text says about the poke-env state-tracking bugs he fixed. Finish with a table: each hyperparameter JOURNEY step 4 proposes to copy | thesis value and location | agrees with JOURNEY? Note name: wang_thesis.md`,

  wang_pokeenv_fork: `Read ${PW}/wang_fork_diffs.md section 2 (lines 3409-3992: the quadraticmuffin/poke-env fork commit log and the cumulative diff vs upstream merge-base 5afb63b). Enumerate EVERY change (the index says ~36 state-tracking fixes: Max PP, Sleep Talk double-decrementing, weather-from-abilities persistence, sleep counters, Trace base-ability parsing, maybe_trapped, _force_switch as a list, and more). For each: what it fixes (gen4-specific or general; what a wrong observation would look like to a policy), the file / function, and then CHECK the pinned poke-env 0.15.0 source under ${PE} to classify it: UPSTREAMED (equivalent code present; cite PE file:line), PARTIAL (what is there and what is missing), ABSENT (0.15.0 still has the behaviour; a gen4 observation-fidelity risk; say what a training run would observe), or N/A (instrumentation or MCTS-only). Produce one table with every change. Then read section 3 (lines 3993 to end, the stable-baselines3 fork) and state what it changes: the index says instrumentation only — verify, and note anything that alters PPO semantics (LR schedule, advantage handling, value clipping, entropy). Note name: wang_pokeenv_fork.md`,

  showdown_gen4_abilities_items: `Abilities and items in gen4randombattle, restricted to what can actually appear. First derive the ability universe from ${SD}/data/random-battles/gen4/sets.json (every 'abilities' entry, deduplicated, with counts) and the item universe from the generator ${SD}/data/random-battles/gen4/teams.ts plus the inherited ${SD}/data/random-battles/gen5/teams.ts logic (enumerate every item literal that can be assigned and the condition under which it is). Then for each ability and item, using ${SD}/data/abilities.ts, ${SD}/data/items.ts, and the gen4 mod overrides ${SD}/data/mods/gen4/{abilities.ts,items.ts} (follow the inherit chain so gen4 semantics win), record: the mechanical effect in gen4 terms (file:line), whether it changes a quantity an encoder should carry (speed, damage multiplier, type immunity, trapping, weather, stat boosts, status immunity, per-turn recovery or chip, PP, priority, accuracy, choice lock), whether and when it is REVEALED to the opponent (ability activation messages; item reveal on use, Frisk, Trick, Knock Off; abilities hidden until they announce), and the protocol message class poke-env would see. Classify abilities into effect classes an encoder could use as features alongside or instead of a raw id: type immunity (Levitate, Flash Fire, Volt Absorb, Water Absorb, Motor Drive, Dry Skin, Wonder Guard), damage modifiers (Thick Fat, Heatproof, Filter, Solid Rock, Technician, Adaptability, Tinted Lens, Huge Power, Pure Power, Guts, Hustle, Iron Fist, Reckless, Rivalry, the pinch boosts Blaze / Torrent / Overgrow / Swarm, Sniper, Super Luck), speed (Speed Boost, Swift Swim, Chlorophyll, Quick Feet, Unburden, Slow Start), status immunity (Insomnia, Vital Spirit, Immunity, Limber, Water Veil, Magma Armor, Own Tempo, Oblivious, Inner Focus, Shield Dust, Soundproof, Clear Body, White Smoke, Hyper Cutter, Keen Eye, Leaf Guard), weather setters (Drizzle, Drought, Sand Stream, Snow Warning) and weather users, trapping (Arena Trap, Shadow Tag, Magnet Pull), contact punishers (Static, Flame Body, Effect Spore, Rough Skin, Poison Point, Aftermath, Cute Charm), recovery / chip (Natural Cure, Shed Skin, Poison Heal, Rain Dish, Ice Body, Hydration, Magic Guard, Solar Power, Dry Skin), information (Trace, Anticipation, Forewarn, Frisk, Download, Intimidate), misc (Mold Breaker, Pressure, Truant, Sturdy with gen4 semantics, Serene Grace, Skill Link, Marvel Scale, Multitype, Flower Gift, Forecast, No Guard, Compound Eyes, Unaware, Simple, Klutz, Normalize, Stall, Tangled Feet, Scrappy, Early Bird, and Lightning Rod / Storm Drain gen4 semantics: redirect only, no immunity — verify in the mod). Same for items: Choice Band / Specs / Scarf lock semantics and reveal, Life Orb, Leftovers, Black Sludge, Focus Sash, Lum / Sitrus / Chesto and the pinch berries, Toxic Orb / Flame Orb, Light Clay, Expert Belt, Wise Glasses / Muscle Band, type plates and type-boost items, Damp / Heat / Smooth / Icy Rock, Iron Ball, Macho Brace, Shed Shell, Wide Lens, Scope Lens, Metronome, Quick Claw, Big Root, Zoom Lens, King's Rock / Razor Fang, Griseous / Adamant / Lustrous Orb, Thick Club, Light Ball, DeepSeaTooth, Shell Bell; exclude gen5-only items (Eviolite, Air Balloon, gems) and say so. Deliver two tables (abilities, items) with counts plus a proposed feature-class taxonomy with the number of classes, and note which abilities / items in the pool have NO encoder-relevant effect (a raw id embedding is then the only sensible carrier). Note name: showdown_gen4_abilities_items.md`,

  project_record: `Work from the project record ONLY, in the snapshot ${SNAP} (= main @2738025) plus two gitignored on-disk files in MAIN. Files: ${SNAP}/SESSION_LOGS.md (index it with grep -n '^- 20' then read entries by offset/limit; never a broad keyword grep; the 2026-08-03 prior-work entries, the 2026-08-23 and 2026-08-26 anchor-battery entries, the 2026-09-04 entries for the 100M grade and the audit close and the gen4-design state), ${SNAP}/RESULTS.md (§16 ladder, §17 R2, §18 the 100M readout: cell P3, S-SHAPE SS-CLIMB, anchors, disclosures), ${SNAP}/CHAPTER5.md (§3 licensed A2, §6, §7 rulings), ${SNAP}/docs/CLEANUP.md, ${SNAP}/readouts/*.md, ${SNAP}/configs/eval/fp_budget_ladder.yaml, ${SNAP}/configs/eval/ch3_r4_fp_anchor.yaml, ${SNAP}/configs/showdown_sp_100m.yaml (header only), ${SNAP}/docs/IDEAS_POST_100M.md §2.5 and §2.6, ${SNAP}/JOURNEY.md, ${SNAP}/STATUS.md, ${SNAP}/docs/archive/AUDIT_BRANCH_LOG.md (the F-08 section and 'Open questions for the maintainer'), and ${MAIN}/results/design_ch5_100m/synthesis_100m.md and BRIEF_100M.md (results/design_ch5_100m is not runs/, logs/ or results/ladder; reading it is allowed). DO NOT read configs/eval/ladder_r4.yaml or anything under results/ladder (the live run is blind and irrelevant here). Assemble with citations (file:line, or entry date plus line): (1) everything recorded about Wang 2024: the 2026-08-03 verification entries (which claims were verified or corrected: hyperparameters, encoder, action-space size, MCTS, LR ablation, the 0.786-vs-0.85 discrepancy, the 'poke-env fixes upstreamed by 0.15.0' claim and its evidence) and every later mention; (2) the anchor-battery convention verbatim: the 2026-08-23 ruling, the MU-2 amendment of 2026-08-26, the locked vs-SH protocol, the BC-clone h2h 500 protocol (what exactly the BC clone is: which teacher, which data, how built — scripts/make_bc_dataset.py and the readouts), the FP@20 protocol and its two standing disclosures verbatim, the FP budget ladder, 'match the policy form to the rating', 'anchors are descriptive never verdict inputs', the ladder-plus-Foul-Play pairing rule, and the disclosure lines that travel with every headline number (N-TIMER, G9 delta, A-COLL, N-ANNEAL) as used in RESULTS §18; (3) the search-depreciation data points with full provenance: the 12M per-lane search deltas +0.051 / +0.104 / +0.148 (lanes, seeds, checkpoints, search form and budget, n, the pooled 0.79283 vs greedy 0.71825), the 50M-era search reads (LADDER R3's object: one-ply search@M on lane s80; RESULTS §16 rows; R4S66 search@20 0.38067 vs greedy 0.4740 off-FP and any vs-SH counterpart), the Chapter 3 close numbers, the 'search substitutes for a deficient value head' hypothesis text (JOURNEY plus the CH3 / D18 / D22 critic diagnostics and the Brier decomposition), and whether ANY search read of the 100M final exists in §18 (if not, say the endpoint is still pending); (4) most-damage-typed: any recorded ruling or spec beyond JOURNEY and IDEAS §2.6; how MaxBasePowerPlayer is used today (rl/envs/showdown.py OPPONENT_PLAYERS; any eval config); (5) the ladder-object question (greedy vs searched; the 100M ladder R4 object is greedy per STATUS) and the standing maintainer rulings touching gen4 or cross-generation transfer ('Weights never transfer between generations': ruling or JOURNEY note?), the 2026-08-23 ladder and big-runs rulings, the pre-reg conventions (the credit line verbatim, the five pre-reg rules, the journey_step header requirement, the 2-Opus cycle rule for irreversible artifacts), STATUS's note that IDEAS_POST_100M must be re-ranked under SS-CLIMB; (6) the 2-Opus house style for adjudications: quote 2-3 adjudication entries from synthesis_100m.md so a writer can imitate the form (recommendation, losing argument, carried-to-ratification, refusal lists). Note name: project_record.md`,

  huang_lee_metagrok: `Huang & Lee 2019 (gen7randombattle pure PPO self-play, Glicko 1677 / 72% GXE, no search) as an encoder and anchor reference for gen4. Sources: the index entry in ${PW}/README.md (grep -n 'huang_lee' and read the whole entry first: it is the verified record and refutes several external readings), ${PW}/HUANG_LEE_DEEP_READ.md, the paper text ${RD}/_huanglee_paged.txt (Table I, Fig. 1, §III, §IV, the bot table; the PDF is ${PW}/huang_lee_2019_selfplay_pokemon.pdf if a figure must be viewed), and the metagrok clone ${MG} (find the feature / encoder code: grep -rn for ability, item, weather, volatile, lastmove, ppUsed, sideCondition, embedding under ${MG}/metagrok and ${MG}/js; the committed run config ${MG}/expts/01.json; the scripted bots including the 'most damage' / 'most damage typed' bot: find its exact algorithm — how it picks the move (base power x type effectiveness x STAB? accuracy? category? stats? does it ever switch?) and cite file:line). Deliver: (1) their full observation feature table with dims and encodings for gen7: species / move / item / ability embeddings and vocab sizes, status (28), volatiles (23), side conditions, weather, lastmove (731-cat), ppUsed, the 'Possible Ability 1/2/3' belief, HP and stat handling, how unrevealed opponent slots are represented, what is Markov-restoring; (2) which of those features are gen4-applicable and which are gen7-only (Z-moves, megas, terrain); (3) the max-damage-typed bot definition at verbatim-level precision, the reported 0.829 against it, and everything the index says about the bot table not transferring; (4) recipe facts relevant to a gen4 pre-reg: both-seat harvest, m=7680 as 'a completely arbitrary choice', gamma 0.95 / lam 0.9, the 5-term zero-sum shaping, no entropy bonus, the ladder-trajectory protocol (300 matches every 100 iterations), and the negative results (Trick Room never learned; fine-tune collapse). Note name: huang_lee_metagrok.md`,

  wang_showdown_fork: `Read ${PW}/wang_fork_diffs.md section 1 (lines 13-3408: the quadraticmuffin/pokemon-showdown fork). Extract with line citations: (a) the MCTS infrastructure: the >getstate / >load stream commands (what is serialized; whether upstream Showdown already serializes battles — the index says serialization itself is upstream: check ${SD}/sim for toJSON / fromJSON / State and cite), the constrained team regeneration (randomConstrainedMoveset / randomConstrainedSet: how he infers the opponent's possible sets from revealed information — locked Hidden Power type, abilities, items, sun / rain assumptions for ability culling, pre-populated teamDetails from locked mons, the 'if no sets are compatible re-allow all' fallback), the hallucinated-move disabling under Encore / Taunt, the isDoubles removal, the username hack; (b) what this reveals about gen4 randbats SET STRUCTURE that an opponent set prior would need: our gen1 encoder has one (${SNAP}/rl/envs/randbats_prior.py and ${SNAP}/rl/envs/data/gen1_randbats_sets.json — read both and compare: what a gen4 equivalent must carry (items, abilities, roles, Hidden Power type, formes), and whether his constrained regen uses the same sets.json structure as the vendored ${SD}/data/random-battles/gen4/sets.json); (c) which parts would be required for a gen4 SEARCH anchor (a Foul-Play-equivalent) and which are not needed for a pure-policy chapter; (d) drift: how much of this fork targets the 2023-12 Showdown vs the vendored 0.11.11 — check whether ${SD}/data/random-battles/gen4/teams.ts still has the functions / fields he modified (randomSet, randomMoveset, teamDetails, movepool structure) and name what moved. Note name: wang_showdown_fork.md`,

  psppo_metamon_obs: `How the two largest published pure-policy systems encode items, abilities, weather, hazards, and multi-turn state, as design references for a gen4 encoder (they are gen9 and gen1-4-OU systems; extract only what transfers). Sources: ${PSPPO}/obs_abilities.py, obs_pokemon.py, obs_moves.py, obs_global.py, obs_assembler.py, obs_transitions.py, vocab.json, type_schema.json, config.py (HEAD), plus the 1b13ae0-era copies in ${RD}/psppo_1b13ae0/ (the 2102-Elo ladder era) — read the index entry for ps-ppo in ${PW}/README.md FIRST (grep -n 'ps-ppo'): it records claims that are wrong and two dead-code defects (a Target-enum comparison that never fires; dead LR multipliers); do not repeat them as facts. And Metamon: ${RD}/_metamon_paged.txt (the observation-space / tokenization appendix: text-feature and numeric-feature lists, how items and abilities are tokenized for gens 1-4, the action space of 9 or 13, any statement about which features were necessary; read the index entry too). Deliver: (1) ps-ppo's per-entity feature lists with dims: how abilities are encoded (obs_abilities.py: embedding? class features? both?), item encoding and its reveal state, the weather / field / side-condition global block, boosts as 7x13 one-hot, stats as (min, est, max) belief ranges, the move token fields, the action mask fed as an observation feature, positional identity fields, and what obs_transitions.py adds (temporal context) — with the HEAD-vs-1b13ae0 caveats; (2) Metamon's equivalents; (3) a comparison table: feature | ps-ppo | Metamon | H&L (from the index entry) | our gen1 encoder (${SNAP}/rl/envs/encoder_spec.py GEN1 + ${SNAP}/rl/envs/showdown.py block layout) — and the design options it implies for gen4 (embed raw ids vs effect-class features vs both; precomputed type effectiveness vs raw types). Cite file:line and pages. Note name: psppo_metamon_obs.md`,

  foulplay_pokeengine_gen4_pokejax: `Is there a Foul-Play-equivalent search anchor for gen4randombattle, and what does pokejax teach about a gen4 engine? Sources: the foul-play clone ${FP} (fp/ code: format parsing and gen handling — grep -rn for 'gen4', 'generation', 'gen ==' ; fp/data/sets and fp/data/pkmn_sets_cache for gen4randombattle set files; fp/data/mods; requirements.txt for the poke-engine pin; Makefile for the feature-flag build); the poke-engine feature flags — locate poke-engine on disk read-only: 'conda env list' is fine, then look for site-packages/poke_engine under that env, and check for any Cargo.toml checkout under /Users/nickgreenquist/Documents/Projects (find -maxdepth 4 -name Cargo.toml); the index entry for foul-play in ${PW}/README.md (it records: pinned poke-engine 0.0.48, feature flags gen1..gen9 with default=[] verified in Cargo.toml, the gen1-build proof via module paths, the gen9-engine-in-gen1 A/B that panicked, our patch); our patch ${SNAP}/scripts/patches/foulplay_gen1_local.patch (what was gen1-specific and what a gen4 build would still need — login, process pool, tape writer); ${SNAP}/docs/landmines.md sections 'Foul-Play runner ops' and 'THE ORPHANED-ROOM DEADLOCK' (the Struggle panic 'Invalid PokemonMoveIndex: 4' in poke-engine src/state.rs — would gen4 hit it? what can you say from source); ${SNAP}/configs/eval/fp_budget_ladder.yaml and ${SNAP}/configs/eval/ch3_r4_fp_anchor.yaml (the FP@20 protocol and its two standing disclosures — quote verbatim); ${SNAP}/scripts/ch3_fp_h2h.py and ${SNAP}/scripts/ch3_r4_fp_runner.sh (what is gen1-specific in the harness); and ${PW}/pokejax/{ANALYSIS_local_heuristic_winrate.md,ANALYSIS_winrate_gap.md,local_summary.json,training_log.txt} plus the index's pokejax entry (the obs-bridge bug list: stale available_moves after switches on 15.9% of turns, PP never decrementing locally, sleep-turn off-by-one). Deliver: (1) a feasibility assessment: does poke-engine expose a gen4 feature flag; does foul-play have gen4 mechanics and set support (evidence: code paths, set files, tests); what a 'make poke_engine GEN=gen4' would require; what needs live verification (a gen4 smoke battle vs SH, post-ladder) and the exact check; the Struggle / turn-1000 risk for gen4 (stall wars with Leftovers / Protect / Toxic are common in gen4 — assess qualitatively and point at the Endless Battle Clause); (2) Wang's MCTS as the alternative search anchor: cite only what the index says (another agent reads the thesis) and what infrastructure it needs; (3) pokejax's gen4 engine choices and its bug list turned into an audit checklist for OUR gen4 bridge (each item: what to check in poke-env 0.15.0, from source now or live later). Note name: foulplay_pokeengine_gen4_pokejax.md`,

  mechanics_literature: `Build the gen1 -> gen4 rules delta from the LITERATURE side (Bulbapedia generation-mechanics pages, Smogon's DPP resources, Showdown's own docs and sim comments on GitHub), as an independent cross-check of the data-file agents. For each item give the literature claim, the URL, and the exact vendored-data location where it should be verified (${SD}/data/mods/gen4/..., ${SD}/data/conditions.ts, ${SD}/data/moves.ts, ${SD}/data/typechart.ts, ${SD}/sim/...); verify in the data file when it is cheap (a grep) and tag tree-verified when you did, literature-only when you did not. Cover: the physical/special split (gen4 per-move category; gens 1-3 by type); types 15 -> 17 (Dark and Steel added in gen2; the specific type-chart cells that changed from gen1: Ghost vs Psychic 0x -> 2x, Bug vs Poison 2x -> 0.5x, Poison vs Bug 2x -> 1x, Ice vs Fire 1x -> 0.5x; the Steel resistances; Dark's immunity to Psychic; Ghost / Dark vs Steel); held items (gen2+; which classes matter); abilities (gen3+); weather (gen2+; ability weather gen3+; gen4 ability weather lasts until replaced); entry hazards (Spikes gen2, Stealth Rock and Toxic Spikes gen4); status changes (sleep duration by generation and whether the wake turn allows a move; freeze thaw chance by generation; paralysis speed factor; burn; Toxic counter behaviour on switch; confusion; Attract); volatiles and moves new in gens 2-4 that shape play: Substitute (gen1 vs gen4 behaviour), Protect / Detect, Encore, Taunt, Trick, U-turn, Pursuit, Roar / Whirlwind, Rapid Spin, Baton Pass, Perish Song, Wish, Rest / Sleep Talk, Toxic, Will-O-Wisp, Thunder Wave, the Stealth Rock damage table, Explosion / Selfdestruct halving Defense in gens 1-4, Hidden Power; critical-hit mechanics (gen1 speed-based vs gen2+ stages; gen4 base rate 1/16; multiplier 2x; high-crit moves); accuracy / evasion; priority brackets and speed ties; PP and Pressure; stat mechanics (gen1 single Special vs SpA/SpD; natures / EVs / IVs from gen3 and what randbats does with them); team preview (introduced in gen5, so gens 1-4 have none); the Endless Battle Clause; Struggle recoil by generation; Rest turns; Roost; Trick Room; Gravity; multi-hit distributions; OHKO moves and clauses; the Sleep Clause Mod; the Freeze Clause. Also flag which gen2 / gen3-era additions an encoder for gen4 must carry even though they are not gen4-new, and say so explicitly. Note name: mechanics_literature.md`,
}

const WAVES = [
  { title: 'Wave 1', keys: ['our_encoder_seam_inventory', 'showdown_gen4_pool', 'pokeenv_battle_state', 'pokeenv_env_layer', 'showdown_gen4_mechanics'] },
  { title: 'Wave 2', keys: ['wang_thesis', 'wang_pokeenv_fork', 'showdown_gen4_abilities_items', 'project_record', 'huang_lee_metagrok'] },
  { title: 'Wave 3', keys: ['wang_showdown_fork', 'psppo_metamon_obs', 'foulplay_pokeengine_gen4_pokejax', 'mechanics_literature'] },
]

const done = []
const missing = []
for (const w of WAVES) {
  phase(w.title)
  log(w.title + ': ' + w.keys.join(', '))
  const res = await parallel(w.keys.map(k => () =>
    agent(PREAMBLE + T[k], { ...OPTS, label: 'research:' + k, phase: w.title, schema: RESEARCH_SCHEMA, agentType: k === 'project_record' ? 'doc-archaeologist' : undefined })
      .then(r => r ? { key: k, ...r } : null)
  ))
  res.forEach((r, i) => { if (r) done.push(r); else missing.push(w.keys[i]) })
  log(w.title + ' done: ' + res.filter(Boolean).length + '/' + w.keys.length + (missing.length ? ' (missing so far: ' + missing.join(', ') + ')' : ''))
}

phase('Reconcile')
const CRITIC_SCHEMA = {
  type: 'object',
  required: ['index_path', 'conflicts', 'gaps', 'followups'],
  properties: {
    index_path: { type: 'string' },
    conflicts: { type: 'array', items: { type: 'object', required: ['topic', 'positions', 'settle_by'], properties: { topic: { type: 'string' }, positions: { type: 'string' }, settle_by: { type: 'string' } } } },
    gaps: { type: 'array', items: { type: 'string' } },
    followups: { type: 'array', items: { type: 'object', required: ['name', 'prompt'], properties: { name: { type: 'string' }, prompt: { type: 'string' } } } },
  },
}
const critic = await agent(PREAMBLE + `You are the RECONCILE critic, not a source agent. Read EVERY research note under ${RD} (ls ${RD}; read each .md whose name does not start with an underscore, in full). Agents that did not complete (their notes may be absent or partial): ${missing.length ? missing.join(', ') : 'none'}. Produce ${RD}/INDEX.md containing: (1) a one-paragraph abstract per note with its path; (2) a CONFLICTS table: every place two notes disagree on a fact (sleep turn counts, Wang hyperparameters, vocab sizes, poke-env field semantics, SH behaviour, item lists, ability semantics, anything): quote both positions with their citations, name which has the stronger verification status, and say what would settle it; (3) a GAPS list against the six deliverables (mechanics_delta, pokeenv_gen4_survey, encoder_requirements, anchors_and_eval, open_questions, search_depreciation): claims still literature-only that could be tree- or source-verified cheaply; sources named in the brief that no note actually read (check each: Wang thesis Tables A.1 / A.2 / A.3 and §3.1.4 / §5.1.3, wang_fork_diffs.md all three sections, the H&L text and the metagrok bot code, all four pokejax files, poke-env singles_env / env / battle / pokemon / move / baselines, all eight SD/data/mods/gen4 files, both random-battles/gen4 files and the gen5 base they inherit, the SD/config/formats.ts gen4randombattle entry, the landed encoder_spec.py and test_encoder_spec.py, the F-08 landing record, our rl/envs/showdown.py in full, IDEAS §2.5 / §2.6, the anchor-battery session-log entries of 2026-08-23 and 2026-08-26, RESULTS §18, ps-ppo obs files, the Metamon appendix); and gen4 mechanics with no owner; (4) up to 8 FOLLOW-UP tasks, each a self-contained agent prompt (a short snake_case name for the note file, the precise question, the exact files or pages to read, what to return) that closes one specific conflict or gap; prioritize load-bearing facts for the encoder and the anchors over completeness for its own sake; if an entire source-family agent is missing, the first follow-ups re-run its task in a reduced form. Return index_path, conflicts, gaps, followups via the schema.`, { ...OPTS, label: 'reconcile-critic', phase: 'Reconcile', schema: CRITIC_SCHEMA })

let followups = []
if (critic && critic.followups && critic.followups.length) {
  phase('Follow-up')
  const fu = critic.followups.slice(0, 8)
  log('Follow-ups: ' + fu.map(f => f.name).join(', '))
  for (let i = 0; i < fu.length; i += 4) {
    const batch = fu.slice(i, i + 4)
    const res = (await parallel(batch.map(f => () =>
      agent(PREAMBLE + `FOLLOW-UP TASK named '${f.name}' from the reconcile critic (its index is at ${RD}/INDEX.md; read the notes it points at as needed):\n\n` + f.prompt + `\n\nNote name: ${f.name}.md`, { ...OPTS, label: 'followup:' + f.name, phase: 'Follow-up', schema: RESEARCH_SCHEMA })
        .then(r => r ? { key: f.name, ...r } : null)
    ))).filter(Boolean)
    followups.push(...res)
    log('Follow-up batch done: ' + res.length + '/' + batch.length)
  }
} else {
  log('No follow-ups named by the critic' + (critic ? '' : ' (critic returned null)'))
}

return { sweep: done, missing, critic, followups }