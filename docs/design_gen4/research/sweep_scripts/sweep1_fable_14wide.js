export const meta = {
  name: 'gen4-research-sweep',
  description: 'Gen4 design: research sweep over Wang diffs/thesis, H&L, poke-env source, vendored Showdown gen4 data, our encoder, anchors; reconcile; follow-ups',
  phases: [
    { title: 'Sweep', detail: 'one agent per source family, structured findings with citations' },
    { title: 'Reconcile', detail: 'index all notes, find conflicts and gaps, name follow-ups' },
    { title: 'Follow-up', detail: 'targeted agents for the gaps the critic named' },
  ],
}

const MAIN = '/Users/nickgreenquist/Documents/Projects/pokemon-showdown-rl'
const WT = '/Users/nickgreenquist/Documents/Projects/pokemon-showdown-rl-gen4design'
const AUDIT = '/Users/nickgreenquist/Documents/Projects/pokemon-showdown-rl-audit'
const PE = '/opt/anaconda3/envs/pokemon-showdown-rl/lib/python3.13/site-packages/poke_env'
const SD = MAIN + '/showdown'
const PW = MAIN + '/prior_work'
const PSPPO = '/Users/nickgreenquist/Documents/Projects/ps-ppo'
const FP = '/Users/nickgreenquist/Documents/Projects/foul-play'
const MG = '/Users/nickgreenquist/Documents/Projects/metagrok'
const SCRATCH = '/private/tmp/claude-501/-Users-nickgreenquist-Documents-Projects-pokemon-showdown-rl/b1478b5b-c556-4e2e-9100-b0db7e234069/scratchpad'
const RD = SCRATCH + '/research'

const PREAMBLE = `You are one research agent in a staged, docs-only design sweep for the GEN 4 chapter (format gen4randombattle) of an RL project that today plays Pokemon Showdown gen1randombattle through poke-env 0.15.0 with a hand-built 828-dim encoder. A 100M-step training fleet is running on this machine right now; your work must be invisible to it.

HARD BARS (violating any one is a session-ending failure):
- READ-ONLY everywhere except SCRATCH (${SCRATCH}). Never edit, create, checkout, stash, or commit anything inside any git tree (MAIN, WT, AUDIT, PSPPO, FP, MG). Never run pip install or conda install. Never contact the Showdown server (port 8000), never start a battle, never construct a poke-env Player/Env/PSClient object, never run rl.train, pytest, or any benchmark. Never kill or signal any process. Never read MAIN/runs or MAIN/logs.
- CPU politeness: grep/sed/cat/jq/pdftotext/head/wc are fine. If you need Python to parse data, run 'nice -n 19 /opt/anaconda3/envs/pokemon-showdown-rl/bin/python' on a short script that touches no network; importing poke_env for data tables (GenData) is allowed, instantiating players/envs is not. 'node -e' for pure data parsing under nice -n 19 is allowed but prefer grep/jq.
- The shell is zsh: quote glob patterns, e.g. grep -rn --include='*.py'.
- Web research (WebFetch/WebSearch; load them with ToolSearch if they are deferred) is allowed for mechanics references (Bulbapedia, Smogon, Showdown docs, GitHub); reddit is unfetchable. A claim that rests on the web is 'literature-only' unless you then verify it in a file on disk.

PATHS (all read-only):
- MAIN = ${MAIN} (the live repo, branch main @ 60c1225; its showdown/ and the big prior_work/ files exist ONLY here because they are gitignored)
- WT = ${WT} (our design worktree; identical code to MAIN at 60c1225; the final docs will be written there later by other agents, not by you)
- AUDIT = ${AUDIT} (the audit-fixes branch; docs/AUDIT_ACTION_PLAN.md finding F-08 = the per-gen EncoderSpec seam, documented but not yet coded)
- PE = ${PE} (installed poke-env 0.15.0 source; data/static/{moves,pokedex,typechart}/gen4*.json)
- SD = ${SD} (vendored pokemon-showdown 0.11.11 @ 59da482: data/, data/mods/gen4/, data/mods/gen1/, data/random-battles/gen4/{sets.json,teams.ts}, sim/, config/formats.ts)
- PW = ${PW} (wang_fork_diffs.md; wang2024_mit_thesis_randbats_rl.pdf; huang_lee_2019_selfplay_pokemon.pdf; grigsby2025_metamon.pdf; pokejax/; HUANG_LEE_DEEP_READ.md; README.md = the VERIFIED prior-work index: read its entry for any system BEFORE citing that system, several popular claims are recorded there as wrong)
- PSPPO = ${PSPPO} (ps-ppo clone), FP = ${FP} (foul-play clone), MG = ${MG} (Huang & Lee's metagrok clone)
- pdftotext, jq, node are on PATH. For a PDF: pdftotext -layout <pdf> ${RD}/_<name>.txt then grep/sed the text; cite page numbers (form feeds separate pages; printed page numbers are in the layout text).

VERIFICATION STATUS VOCABULARY (tag EVERY finding with exactly one):
- tree-verified: checked against a file in the repo tree (rl/, scripts/, configs/, tests/, docs/ under MAIN/WT/AUDIT) or the vendored SD data/sim, i.e. the game as we actually run it.
- source-verified: checked against an external primary source on disk: PE source, Wang's diffs or thesis PDF, the H&L PDF or MG code, PSPPO, FP, Metamon PDF.
- literature-only: from a secondary write-up, a web page, memory, or the prior-work index without re-checking the primary.
- needs-live-verification: only a running server or battle can confirm it; BARRED until post-fleet; say exactly what the check would be.

OUTPUT CONTRACT:
1. Write a complete, self-contained markdown research note to ${RD}/<note-name>.md (the note name is given in your task). Header: agent name, date 2026-09-02, every source you read with path and line ranges or pages, and the status legend above. Body: findings grouped by topic; each finding is a paragraph or table row carrying its status tag and a precise citation (file:line, or PDF page/table/figure). Quote verbatim for load-bearing numbers and formulas. Include, where relevant, a section 'gen1 encoder assumptions this breaks', a section 'open questions for the maintainer', and a section 'unread / unverified' listing anything you did not get to. Be exhaustive: length is not a constraint, correctness is. Never invent a citation; if you did not read it, say so.
2. Return the structured summary (schema enforced): note_path, summary (<= 400 words), key_findings (the 10-30 load-bearing ones, each with status + citation), gen1_assumptions_broken, open_questions, unverified.

Downstream, other agents will write these docs from the notes: mechanics_delta.md (gen1->gen4 rules delta that matters to an encoder and a policy), pokeenv_gen4_survey.md (what the pinned poke-env supports for gen4randombattle), encoder_requirements.md (the gen4 EncoderSpec designed against the audit branch's F-08 seam; gen1's 828-dim layout is untouchable), anchors_and_eval.md (what SimpleHeuristicsPlayer is worth in gen4, the most-damage-typed anchor, a Foul-Play-equivalent for gen4, how the gen1 anchor-battery convention translates), open_questions.md (maintainer rulings, each with a recommendation and the losing argument), search_depreciation.md (stretch). Record cross-references you notice for the other docs, but stay inside your own source family; other agents cover the rest.

YOUR TASK:
`

const RESEARCH_SCHEMA = {
  type: 'object',
  required: ['note_path', 'summary', 'key_findings', 'gen1_assumptions_broken', 'open_questions', 'unverified'],
  properties: {
    note_path: { type: 'string' },
    summary: { type: 'string' },
    key_findings: {
      type: 'array',
      items: {
        type: 'object',
        required: ['claim', 'status', 'citation'],
        properties: {
          claim: { type: 'string' },
          status: { type: 'string', enum: ['tree-verified', 'source-verified', 'literature-only', 'needs-live-verification'] },
          citation: { type: 'string' },
        },
      },
    },
    gen1_assumptions_broken: { type: 'array', items: { type: 'string' } },
    open_questions: { type: 'array', items: { type: 'string' } },
    unverified: { type: 'array', items: { type: 'string' } },
  },
}

const TASKS = [
  { key: 'wang_thesis', prompt: `Read Wang 2024 (Jett Wang, MIT MEng thesis, 'Winning at Pokemon Random Battles Using Reinforcement Learning') in FULL via pdftotext: ${PW}/wang2024_mit_thesis_randbats_rl.pdf. Read the index entry for it in ${PW}/README.md first (grep -n 'wang2024'). Extract, with page/table/figure citations and verbatim numbers: (a) the format and environment (gen4randombattle; the 3v3 surrogate used for Bayesian hyperparameter optimization; any Showdown fork used during training vs only at test time); (b) the observation design in full, Tables A.1/A.2 and the surrounding text: every feature, its encoding (one-hot / bins / scalars), dims, how items, abilities, weather, hazards, volatiles, effect durations, PP and HP are represented (the index records HP binned, PP as floor(pp^(1/3)), multi-turn effect durations as one-hot counters to restore Markovianity), the total observation size, and how unrevealed opponent information is represented; (c) the action space (size; positional or identity-based; the index calls it a '494-way identity space', verify) and masking; (d) the reward; (e) EVERY hyperparameter in Table A.3 verbatim plus the LR schedule formula (the index quotes 10^-4.23/(8x+1)^1.5) and the LR-anneal ablation (§3.1.4: constant 0.55 -> annealed 0.80). RESOLVE A DISCREPANCY and be explicit: the session brief says 'gamma=1.0/lambda=0.95 at 128k steps/update' for Wang, JOURNEY.md (${WT}/JOURNEY.md step 4) says 'gamma 0.9999, lambda 0.754, 7 epochs, clip 0.0829, value clip 0.0184, ent 0.0588, vf 0.4375, grad-norm 0.543, n_steps 78x512, batch 1024, hidden 256, features 896', and IDEAS_POST_100M.md says Wang lambda 0.754 — report exactly what the thesis says, where, and which document is wrong, including steps per update (n_steps x n_envs) and total training steps; (f) training scale (total steps, wall clock, hardware, number of envs), SB3 usage, number of seeds and error bars if any; (g) results: Table 4.1 (0.786 vs SimpleHeuristicsPlayer) vs Figure 4.1 (read as ~0.85; the index flags this as unreconciled — try to reconcile from the text: different checkpoint? n? sampling vs greedy? 3v3 vs 6v6?), the ~0.575 at 6M steps, and the ladder result's exact numbers (rank, Elo, Glicko, GXE, n, whether the laddered agent used MCTS); (h) MCTS at test time: simulations/time budget, determinization of hidden information (how opponent sets are sampled; the constrained team regeneration), how the network's policy/value are used, the reported gain of search over the raw network vs SH and on the ladder; (i) negative results: curriculum (§5.1.3), the bootstrapping/transfer variant (JOURNEY: 'no significant improvement'), anything else; (j) what the thesis text says about the poke-env state-tracking bugs he fixed (another agent reads the code diff). Finish with a table: each hyperparameter JOURNEY step 4 proposes to copy | thesis value and location | agrees with JOURNEY? Note name: wang_thesis.md` },

  { key: 'wang_pokeenv_fork', prompt: `Read ${PW}/wang_fork_diffs.md section 2 (lines 3409-3992: the quadraticmuffin/poke-env fork commit log and the cumulative diff vs upstream merge-base 5afb63b). Enumerate EVERY change (the index says ~36 state-tracking fixes: Max PP, Sleep Talk double-decrementing, weather-from-abilities persistence, sleep counters, Trace base-ability parsing, maybe_trapped, _force_switch as a list, and more). For each: what it fixes (gen4-specific or general; what a wrong observation would look like to a policy), the file/function, and then CHECK the pinned poke-env 0.15.0 source under ${PE} to classify it: UPSTREAMED (equivalent code present; cite PE file:line), PARTIAL (cite what is there and what is missing), ABSENT (0.15.0 still has the behaviour; this is a gen4 observation-fidelity risk; say what a training run would observe), or N/A (instrumentation or MCTS-only). Produce one table with every change. Then read section 3 (lines 3993 to end, the stable-baselines3 fork) and state what it changes: the index says instrumentation only — verify, and note anything that alters PPO semantics (LR schedule, advantage handling, value clipping, entropy). Also grep ${PE} for TODO/FIXME comments near the code his fixes touch. Note name: wang_pokeenv_fork.md` },

  { key: 'wang_showdown_fork', prompt: `Read ${PW}/wang_fork_diffs.md section 1 (lines 13-3408: the quadraticmuffin/pokemon-showdown fork). Extract with line citations: (a) the MCTS infrastructure: the >getstate / >load stream commands (what is serialized; whether upstream Showdown already serializes battles — the index says serialization itself is upstream: check SD/sim for 'toJSON'/'fromJSON'/State and cite), the constrained team regeneration (randomConstrainedMoveset / randomConstrainedSet: how he infers the opponent's possible sets from revealed information — locked Hidden Power type, abilities, items, sun/rain assumptions for ability culling, pre-populated teamDetails from locked mons, the 'if no sets are compatible re-allow all' fallback), the hallucinated-move disabling under Encore/Taunt, the isDoubles removal, the username hack; (b) what this reveals about gen4 randbats SET STRUCTURE that an opponent set prior would need: our gen1 encoder has one (${WT}/rl/envs/randbats_prior.py and ${WT}/rl/envs/data/gen1_randbats_sets.json — read both and compare: what would a gen4 equivalent need to carry (items, abilities, roles, Hidden Power type), and does his constrained regen use the same sets.json structure as the vendored SD/data/random-battles/gen4/sets.json); (c) which parts would be required for a gen4 SEARCH anchor (a Foul-Play-equivalent) and which are not needed for a pure-policy chapter; (d) drift: how much of this fork targets the 2023-12 Showdown vs the vendored 0.11.11 (${SD}): check whether ${SD}/data/random-battles/gen4/teams.ts still has the functions/fields he modified (randomSet, randomMoveset, teamDetails, movepool structure) and name what moved. Note name: wang_showdown_fork.md` },

  { key: 'pokeenv_battle_state', prompt: `Survey the pinned poke-env 0.15.0 battle-state model for gen4 under ${PE}: battle/abstract_battle.py, battle/battle.py, battle/pokemon.py, battle/move.py, battle/effect.py, battle/side_condition.py, battle/weather.py, battle/field.py, battle/status.py, battle/move_category.py, battle/target.py, data/gen_data.py, data/normalize.py, and the static data data/static/typechart/gen4typechart.json, data/static/pokedex/gen4pokedex.json, data/static/moves/gen4moves.json. Report with file:line: (1) Pokemon fields and their gen4 semantics: item (the values for unknown / none / known, how it is revealed, what happens on Trick / Knock Off / consumed berries), ability and possible_abilities (how they are set from the pokedex, how revealed, Trace), stats / base_stats (is spd separate; how stats are estimated for the opponent), boosts (7 keys), status and status_counter (sleep and toxic counters: how the counter is maintained from protocol messages — this decides whether an encoder can carry a sleep-turn feature), effects (the Effect enum: list which gen4 volatiles map to a member and which fall to UNKNOWN; count of members; check SUBSTITUTE, PROTECT, ENCORE, TAUNT, TORMENT, LEECH_SEED, CONFUSION, CURSE, PERISH_SONG, YAWN, ATTRACT, INGRAIN, AQUA_RING, MAGNET_RISE, TRAPPED / partial trap, DISABLE, EMBARGO, HEAL_BLOCK, FOCUS_ENERGY, SLOW_START, FLASH_FIRE, DESTINY_BOND, MUST_RECHARGE, and the Light Screen-as-volatile gen1 quirk), must_recharge, preparing (two-turn moves), protect_counter, first_turn, revealed, moves dict (Move objects; current_pp / max_pp: who tracks PP and whether it is decremented locally or parsed from the server), level, gender, the is_dynamaxed / terastallized guards; (2) Battle fields: weather (dict Weather -> turn; how gen4 permanent ability weather appears), fields (the Field enum: TRICK_ROOM, GRAVITY, ...), side_conditions / opponent_side_conditions (the SideCondition enum incl. STEALTH_ROCK, SPIKES, TOXIC_SPIKES, REFLECT, LIGHT_SCREEN, SAFEGUARD, MIST, TAILWIND, LUCKY_CHANT, and how layers are counted), turn, force_switch / _force_switch, maybe_trapped / trapped, can_mega / can_z / can_dynamax / can_tera (inert in gen4), available_moves / available_switches, team_preview handling (gen5+ only: confirm a gen4 randbats battle never gets it), how the opponent's team is revealed and stored, last-move / history data (_replay_data or any event log an encoder could use); (3) Move fields from gen4moves.json: category (PHYSICAL / SPECIAL / STATUS per move, so the by-type rule is dead), base_power, accuracy, priority, pp, secondary, boosts, self_boost, heal, drain, recoil, self_switch, force_switch, volatile_status, side_condition, weather, status, flags, target, is_protect_move, breaks_protect, expected_hits, crit_ratio, sleep_usable, thaws_target, and the SPECIAL_MOVES aliasing (struggle / recharge; gen1's 'fight' placeholder is gen1-only); (4) the gen4 type chart from gen4typechart.json: exactly 17 types (no Fairy; Dark and Steel present), the multiplier semantics and GenData.from_gen(4).type_chart; (5) the damage-calc modules present (calc/damage_calc_gen1_2.py, calc/damage_calc_gen9.py: is there nothing for gen4, and what does that mean for any engineered damage feature); (6) any TODO / FIXME / known-gap comments, and parsing paths where a gen4 protocol message would land in an unknown bucket. For every behaviour that depends on live protocol traffic (what the server actually sends for gen4 items, abilities, weather, hazards), tag needs-live-verification and specify the exact post-fleet check. Note name: pokeenv_battle_state.md` },

  { key: 'pokeenv_env_layer', prompt: `Survey the pinned poke-env 0.15.0 environment / action / player layer for gen4randombattle under ${PE}: environment/singles_env.py, environment/env.py, environment/single_agent_wrapper.py, player/player.py, player/battle_order.py, player/baselines.py (read it in FULL), player/utils.py, ps_client/ps_client.py (format and team handling for a team-less format, the timer command), and data/gen_data.py. Report with file:line: (1) the action space: get_action_space_size(gen=4) — verify it is 10 and give the exact index layout (which indices are switches and which are moves; the ordering of switch slots vs battle.team order; how order_to_action / action_to_order map for gen4 with no mega / z / dynamax / tera branches; how the action mask is built for gen4 and how trapped, force_switch, must_recharge, disabled moves, and Struggle show up in it); (2) how the env handles a gen4 battle start (no team preview), request parsing, and the wrapper's obs / reward hooks, plus the max_concurrent_battles literals and the start_timer_on_battle_start knob (our landmine: every seat sends /timer on); (3) SimpleHeuristicsPlayer end to end: every branch and every gen-gated condition — the ENTRY_HAZARDS dict and how it is used, _estimate_matchup (type chart via GenData.from_gen(battle.gen)), _should_switch_out, _stat_estimation (the +1-boost bug the index mentions ps-ppo patched: describe it exactly), the speed-tier logic, the setup-move logic (the index calls SH's setup branch 'dead': find out why), dynamax / tera guards (inert for gen4), the move-scoring formula (base power, type multiplier, STAB, accuracy, expected hits, physical/special handling — does it read move.category or infer from type?), and also MaxBasePowerPlayer and RandomPlayer. Conclude concretely which SH branches are INERT in gen1 and LIVE in gen4 (JOURNEY's argument that SH strength drifts across generations), and whether anything makes SH relatively weaker in gen4 (e.g. it ignores items / abilities / weather / hazard damage on switch-in?); (4) anything else in this layer that is gen-specific and would differ between gen1 and gen4 (SPECIAL_MOVES aliasing, the 'fight' placeholder, struggle, recharge). Tag live-protocol-dependent claims needs-live-verification with the exact post-fleet check. Note name: pokeenv_env_layer.md` },

  { key: 'showdown_gen4_pool', prompt: `Characterize the gen4 random-battle team pool as vendored: ${SD}/data/random-battles/gen4/sets.json and ${SD}/data/random-battles/gen4/teams.ts (it extends ../gen5/teams — read ${SD}/data/random-battles/gen5/teams.ts and any further base it inherits for item assignment, EVs / IVs / nature, Hidden Power, level scaling, team constraints), the ${SD}/config/formats.ts entry for gen4randombattle (ruleset; confirm whether Team Preview is absent), ${SD}/data/rulesets.ts for the named rules, and ${SD}/data/mods/gen4/formats-data.ts if the generator reads it. Deliver exact counts with the commands used (jq or a nice'd python one-off): number of species keys in sets.json; number of distinct moves across all movepools; number of distinct abilities; the set of items the generator can assign (enumerate every item literal in teams.ts and the inherited base; mark which are gen4-legal); level range and distribution; the roles list; sets per species; whether movepools are exact 4-move sets or pools that are sampled (and the sampling constraints: MOVE_PAIRS, NO_STAB, HAZARDS, SETUP, priority rules); how the ability is chosen when a set lists several; the EV / IV / nature policy and the Hidden Power IV rule; team-level constraints (type caps, weakness caps, teamDetails counters for hazards, Rapid Spin, weather, screens); the rules applying to randbats from the format entry (Sleep Clause? Endless Battle Clause? HP Percentage Mod, Cancel Mod). Compare against the gen1 pool (${SD}/data/random-battles/gen1/; the index says ~146 species) for scale. Then derive VOCAB SIZES for an encoder: species (pool plus formes that can appear mid-battle, e.g. Rotom formes, Giratina-Origin, Castform, Cherrim, Arceus plates, Wormadam — check what the pool contains), moves (pool moves plus Struggle plus every Hidden Power type variant plus any move a mon can end up with mid-battle — in randbats both sides draw from the same pool, so pool = universe; say so and note exceptions like Metronome / Sleep Talk / Mimic / Transform / Ditto), abilities, items. Our gen1 encoder emits ids as id/256 in float32: flag every vocab that exceeds 255. Note name: showdown_gen4_pool.md` },

  { key: 'showdown_gen4_mechanics', prompt: `Catalogue gen4 battle mechanics AS IMPLEMENTED in the vendored Showdown, focusing on what differs from gen1 and what an encoder or policy must represent. Sources: ${SD}/data/mods/gen4/{scripts.ts,conditions.ts,moves.ts,rulesets.ts,pokedex.ts} (overrides), the base ${SD}/data/{conditions.ts,scripts.ts,moves.ts,typechart.ts,rulesets.ts} (base = current-gen semantics; a gen4 battle uses base data unless the gen4 mod or an intermediate mod in its inherit chain overrides it: READ the gen4 mod's 'inherit' field in scripts.ts and follow the chain gen4 -> gen5 -> ... so that you attribute each mechanic to the right file), and ${SD}/data/mods/gen1/{scripts.ts,conditions.ts,moves.ts,typechart.ts} for the gen1 side of each delta; ${SD}/sim/battle-actions.ts and sim/pokemon.ts for damage, crit, and turn-order code when needed. Cover each with file:line and a status tag: damage formula and rounding; critical hits (rate, stages, multiplier; gen1 speed-based vs gen4 stage-based; high-crit moves; Focus Energy); physical/special per-move category (the 'category' field in base moves.ts; where the gen1-3 by-type rule lives — check ${SD}/data/mods/gen3 or gen4 scripts for getCategory-style overrides); the type chart (17 types; every cell that differs from gen1: Ghost vs Psychic, Bug vs Poison, Poison vs Bug, Ice vs Fire, the Dark and Steel rows and columns); STAB; accuracy / evasion; status conditions (sleep: turn-count range in gen4 vs gen1, whether the mon can act on the wake turn, Sleep Talk / Rest interaction; freeze: thaw chance; paralysis: speed multiplier and full-para rate; burn; poison; toxic counter and whether it resets on switch; confusion turns and self-hit rate; attract); volatiles and multi-turn state (Substitute HP and behaviour, Protect / Detect success counter, Encore, Taunt, Torment, Disable, Leech Seed, Curse, Perish Song, Yawn, Ingrain, Aqua Ring, Magnet Rise, partial trapping, Mean Look / Block / Spider Web trapping, Bide, Rage, Uproar, Rollout / Ice Ball, Outrage / Thrash / Petal Dance lock plus confusion, two-turn moves such as Fly / Dig / Dive / Bounce / Shadow Force / Solar Beam / Skull Bash / Sky Attack, recharge moves such as Hyper Beam / Giga Impact, Charge, Stockpile, Destiny Bond, Grudge, Embargo, Heal Block, Gastro Acid, Power Trick, Lock-On, Foresight / Miracle Eye / Odor Sleuth, Nightmare, Minimize / Defense Curl flags, Roost typing, Wish, Future Sight / Doom Desire, Baton Pass transfer set, U-turn self-switch timing, Pursuit on a switch, Fake Out first-turn, Sucker Punch, Counter / Mirror Coat / Metal Burst, Endeavor, Trick / Switcheroo, Knock Off, Rapid Spin, Roar / Whirlwind, Haze, Explosion / Selfdestruct Defense-halving in gen4, Struggle recoil, Hidden Power type and power derivation); side conditions (Stealth Rock damage by type effectiveness, Spikes layers and damage, Toxic Spikes layers, effects and Poison-type absorption, Reflect / Light Screen turns and Light Clay, Safeguard, Mist, Tailwind, Lucky Chant); weather (rain / sun / sand / hail effects, durations 5 vs 8 with the rocks, whether ability-set weather is INDEFINITE in gen4, sand / hail chip and the immune types, Sand Veil / Snow Cloak); fields (Trick Room, Gravity); priority brackets and speed ties; PP and Pressure; the Endless Battle Clause and the turn cap; how the Sleep Clause Mod, Freeze Clause, OHKO Clause, Evasion Clause apply to randbats. Also state each gen1 quirk that DISAPPEARS (the 1/256 miss, partial trapping skipping the victim's turn, Hyper Beam no-recharge on KO, Counter / Bide / Substitute oddities, the burn / para stat-drop reset bug, Wrap, Toxic plus Leech Seed stacking, sleep 1-7 turns with no move on the wake turn, freeze permanence, speed-based crits, the single Special stat, the Focus Energy bug). Produce the delta as a table: mechanic | gen1 (file:line) | gen4 (file:line) | what an encoder must carry | what a policy must reason about. Do not cover abilities and items in depth (another agent does) beyond naming the mechanics they hook. Note name: showdown_gen4_mechanics.md` },

  { key: 'showdown_gen4_abilities_items', prompt: `Abilities and items in gen4randombattle, restricted to what can actually appear. First derive the ability universe from ${SD}/data/random-battles/gen4/sets.json (every 'abilities' entry, deduplicated, with counts) and the item universe from the generator ${SD}/data/random-battles/gen4/teams.ts plus the inherited ${SD}/data/random-battles/gen5/teams.ts logic (enumerate every item literal that can be assigned and the condition under which it is). Then for each ability and item, using ${SD}/data/abilities.ts, ${SD}/data/items.ts, and the gen4 mod overrides ${SD}/data/mods/gen4/{abilities.ts,items.ts} (follow the inherit chain so that gen4 semantics win), record: the mechanical effect in gen4 terms (file:line), whether it changes a quantity an encoder should carry (speed, damage multiplier, type immunity, trapping, weather, stat boosts, status immunity, per-turn recovery or chip, PP, priority, accuracy, choice lock), whether and when it is REVEALED to the opponent (ability activation messages; item reveal on use, Frisk, Trick, Knock Off; abilities are hidden until they announce), and note how poke-env would see it (the battle-state agent covers PE in depth; you note the message class). Classify the abilities into effect classes an encoder could use as features alongside or instead of a raw id: type immunity (Levitate, Flash Fire, Volt Absorb, Water Absorb, Motor Drive, Dry Skin, Wonder Guard), damage modifiers (Thick Fat, Heatproof, Filter, Solid Rock, Technician, Adaptability, Tinted Lens, Huge Power, Pure Power, Guts, Hustle, Iron Fist, Reckless, Rivalry, Blaze/Torrent/Overgrow/Swarm pinch boosts, Sniper, Super Luck), speed (Speed Boost, Swift Swim, Chlorophyll, Quick Feet, Unburden, Slow Start), status immunity (Insomnia, Vital Spirit, Immunity, Limber, Water Veil, Magma Armor, Own Tempo, Oblivious, Inner Focus, Shield Dust, Soundproof, Clear Body, White Smoke, Hyper Cutter, Keen Eye, Leaf Guard), weather setters (Drizzle, Drought, Sand Stream, Snow Warning) and weather users, trapping (Arena Trap, Shadow Tag, Magnet Pull), contact punishers (Static, Flame Body, Effect Spore, Rough Skin, Poison Point, Aftermath, Cute Charm), recovery / chip (Natural Cure, Shed Skin, Poison Heal, Rain Dish, Ice Body, Hydration, Magic Guard, Solar Power, Dry Skin), information (Trace, Anticipation, Forewarn, Frisk, Download, Intimidate), misc (Mold Breaker, Pressure, Truant, Sturdy with gen4 semantics, Serene Grace, Skill Link, Marvel Scale, Multitype, Flower Gift, Forecast, No Guard, Compound Eyes, Unaware, Simple, Klutz, Normalize, Stall, Tangled Feet, Scrappy, Early Bird, and Lightning Rod / Storm Drain gen4 semantics: redirect only, no immunity — verify in the mod). Same for items: Choice Band / Specs / Scarf lock semantics and reveal, Life Orb, Leftovers, Black Sludge, Focus Sash, Lum / Sitrus / Chesto and the pinch berries, Toxic Orb / Flame Orb, Light Clay, Expert Belt, Wise Glasses / Muscle Band, type plates and the type-boost items, Damp / Heat / Smooth / Icy Rock, Iron Ball, Macho Brace, Shed Shell, Wide Lens, Scope Lens, Metronome, Quick Claw, Big Root, Zoom Lens, King's Rock / Razor Fang, Griseous / Adamant / Lustrous Orb, Thick Club, Light Ball, DeepSeaTooth, Shell Bell, Leftovers; exclude gen5-only items (Eviolite, Air Balloon, gems) and say you excluded them. Deliver two tables (abilities, items) with counts plus a proposed feature-class taxonomy with the number of classes, and note which abilities/items in the pool have NO encoder-relevant effect (so a raw id embedding is the only sensible carrier). Note name: showdown_gen4_abilities_items.md` },

  { key: 'our_encoder_gen1_inventory', prompt: `Inventory every gen-1-specific assumption and hardcode in OUR tree (use ${WT}, which equals MAIN at 60c1225). Read in FULL: ${WT}/rl/envs/showdown.py (1341 lines), ${WT}/rl/envs/randbats_prior.py, the structure of ${WT}/rl/envs/data/gen1_randbats_sets.json (head / jq keys), ${WT}/rl/networks/entity_deepsets.py (tokenizer, embedding vocab sizes, pointer head), ${WT}/rl/networks/opp_action.py, ${WT}/rl/networks/mlp.py, ${WT}/rl/envs/showdown_async.py, ${WT}/rl/envs/make.py, ${WT}/rl/train.py (faked spaces, ENCODER_FINGERPRINT, format strings), ${WT}/scripts/eval_checkpoint.py, ${WT}/rl/search/{shadow_battle.py,bridge.py,determinize.py,harvest.py,expansion.py} (gen1 assumptions in search), ${WT}/rl/common/evaluation.py, ${WT}/rl/selfplay/pool.py (the opponent's own encode path). Then grep the whole WT tree (rl/, scripts/, configs/, tests/) for gen1, GEN1, gen1randombattle, 'fight', 'spa', 'spd', type_chart, GenData, from_gen, ID_SCALE, 256 (quote patterns; --include='*.py' --include='*.yaml' --include='*.sh'). Then read ${AUDIT}/docs/AUDIT_ACTION_PLAN.md findings F-07, F-08, F-11, F-15, F-20, F-21 and §5, ${AUDIT}/docs/AUDIT_BRANCH_LOG.md, the audit branch's test changes (git -C ${AUDIT} diff main -- tests/ pyproject.toml .gitignore), and ${WT}/tests/test_encoder_ids_tapes.py, test_encoder_v2.py, test_showdown_env.py, test_entity_deepsets.py, test_hl_shaping_tapes.py (the bit-identity and golden tests the seam must keep green; the 'data/' tapes are gitignored and exist only in MAIN/data — do not read them, just record what the tests expect and how the goldens were made). Deliver: (1) a table of every gen1 assumption: file:line | what it assumes | gen4 status (breaks / survives / needs a per-gen table) | which EncoderSpec field would own it; cover at least: GEN1_TYPES (15) and every one-hot sized by it, _STATUS_INDEX (6), _BOOST_KEYS (7 with spa/spd mirrored), _BASE_STAT_KEYS (spd dropped), _VOLATILES (7) and the Light Screen Effect.UNKNOWN note, MUST_RECHARGE handling, _SPECIAL_MOVE_IDS and the 'fight' placeholder aliasing plus the mask-desync recovery, the physical-by-type rule in _fill_move, the 23-dim effect block's source fields (_effect_block) and its gen1 move-data dependencies, _spe_est / _speed_edge formulas (which stat formula, which level assumption), _best_multiplier and the type-chart source, the set prior (gen1_randbats_sets.json keyed by species; conditional_move_probs; known_species; what a gen4 version needs: items, abilities, roles, formes), ID_SCALE 256 and _species_id / _move_id assignment (what tables index them, their max ids, and whether a gen4 vocab of moves > 255 breaks id/256 encoding inside the declared Box(-1, 4) and float32 exactness — compute what still works), ENCODER_FINGERPRINT fields, the OBS_DIM composition (verify 828 = 6 + 6*33 + 16 + 4*46 + 6*34 + 16 + 4*46 + 20 from the constants), the tokenizer's 21-token layout and where species_vocab / move_vocab come from (configs? constants?), opp_action's L6 over 4 move slots, privileged_block, hl_event_sum weights and the events it parses, calc_reward, battle_outcome, the faked Discrete(10) sites, the battle_format strings in configs / scripts / tests, the search module's gen1 determinization and any gen1 set logic; (2) the exact F-08 seam text from the audit plan and a statement of what the seam must guarantee (828-dim gen1 bit-identity under which tests and which env vars); (3) a proposed EncoderSpec interface sketch grounded in the code you read: fields (gen, format, types tuple, type chart source, status index, boost keys, base-stat keys, volatile list, side-condition list, weather list, field list, move-category source, special-move ids, vocab tables with sizes and id scale, set-prior loader, block dims and OBS_DIM derivation, fingerprint) and every construction site that would consume it, flagged as a PROPOSAL to reconcile with the audit branch at merge. Note name: our_encoder_gen1_inventory.md` },

  { key: 'huang_lee_metagrok', prompt: `Huang & Lee 2019 (gen7randombattle pure PPO self-play, Glicko 1677 / 72% GXE, no search) as an encoder and anchor reference for gen4. Sources: the index entry in ${PW}/README.md (grep -n 'huang_lee' and read the whole entry first: it is the verified record and refutes several external readings), ${PW}/HUANG_LEE_DEEP_READ.md, the PDF ${PW}/huang_lee_2019_selfplay_pokemon.pdf via pdftotext (Table I, Fig. 1, §III, §IV, the bot table), and the metagrok clone ${MG} (find the feature / encoder code: grep -rn for ability, item, weather, volatile, lastmove, ppUsed, sideCondition, embedding under ${MG}/metagrok and ${MG}/js; the committed run config ${MG}/expts/01.json; the scripted bots including the 'most damage' / 'most damage typed' bot: find its exact algorithm — how it picks the move (base power x type effectiveness x STAB? accuracy? category? stats? does it ever switch?) and cite file:line). Deliver: (1) their full observation feature table with dims and encodings for gen7: species / move / item / ability embeddings and vocab sizes, status (28), volatiles (23), side conditions, weather, lastmove (731-cat), ppUsed, the 'Possible Ability 1/2/3' belief, HP and stat handling, how unrevealed opponent slots are represented, what is Markov-restoring; (2) which of those features are gen4-applicable and which are gen7-only (Z-moves, megas, terrain); (3) the max-damage-typed bot definition at verbatim-level precision, the reported 0.829 against it, and everything the index says about the bot table not transferring; (4) recipe facts relevant to a gen4 pre-reg: both-seat harvest, m=7680 as 'a completely arbitrary choice', gamma 0.95 / lam 0.9, the 5-term zero-sum shaping, no entropy bonus, the ladder-trajectory protocol (300 matches every 100 iterations), and the negative results (Trick Room never learned; fine-tune collapse). Note name: huang_lee_metagrok.md` },

  { key: 'psppo_metamon_obs', prompt: `How the two largest published pure-policy systems encode items, abilities, weather, hazards, and multi-turn state, as design references for a gen4 encoder (they are gen9 and gen1-4-OU systems; extract only what transfers). Sources: ${PSPPO}/obs_abilities.py, obs_pokemon.py, obs_moves.py, obs_global.py, obs_assembler.py, obs_transitions.py, vocab.json, type_schema.json, config.py — read the index entry for ps-ppo in ${PW}/README.md FIRST (grep -n 'ps-ppo'): it records claims that are wrong and two dead-code defects (a Target-enum comparison that never fires; dead LR multipliers); do not repeat them as facts. And Metamon: ${PW}/grigsby2025_metamon.pdf via pdftotext (the observation-space / tokenization appendix: the text-feature and numeric-feature lists, how items and abilities are tokenized for gens 1-4, the action space of 9 or 13, any statement about which features were necessary; read the index entry too). Deliver: (1) ps-ppo's per-entity feature lists with dims: how abilities are encoded (obs_abilities.py: embedding? class features? both?), item encoding and its reveal state, the weather / field / side-condition global block, boosts as 7x13 one-hot, stats as (min, est, max) belief ranges, the move token fields, the action mask fed as an observation feature, positional identity fields, and what obs_transitions.py adds (temporal context) — note commit caveats from the index (HEAD vs the 2102-Elo 7fb522c-era system); (2) Metamon's equivalents; (3) a comparison table: feature | ps-ppo | Metamon | H&L (from the index entry) | our gen1 encoder (${WT}/rl/envs/showdown.py lines 60-185, the block layout) — and the design options it implies for gen4 (embed raw ids vs effect-class features vs both; precomputed type effectiveness vs raw types). Cite file:line and PDF pages. Note name: psppo_metamon_obs.md` },

  { key: 'foulplay_pokeengine_gen4_pokejax', prompt: `Is there a Foul-Play-equivalent search anchor for gen4randombattle, and what does pokejax teach about a gen4 engine? Sources: the foul-play clone ${FP} (fp/ code: format parsing and gen handling — grep -rn for 'gen4', 'generation', 'gen ==' ; fp/data/sets and fp/data/pkmn_sets_cache for gen4randombattle set files; fp/data/mods; requirements.txt for the poke-engine pin; Makefile for the feature-flag build); the poke-engine feature flags — locate poke-engine on disk read-only: 'conda env list' is fine, then look for site-packages/poke_engine under that env, and check for any Cargo.toml checkout under /Users/nickgreenquist/Documents/Projects (find -maxdepth 4 -name Cargo.toml); the index entry for foul-play in ${PW}/README.md (it records: pinned poke-engine 0.0.48, feature flags gen1..gen9 with default=[] verified in Cargo.toml, the gen1-build proof via module paths, the gen9-engine-in-gen1 A/B that panicked, and our patch); our patch ${WT}/scripts/patches/foulplay_gen1_local.patch (read it: what was gen1-specific and what a gen4 build would still need — login, process pool, tape writer); ${WT}/docs/landmines.md sections 'Foul-Play runner ops' and 'THE ORPHANED-ROOM DEADLOCK' (the Struggle panic 'Invalid PokemonMoveIndex: 4' in poke-engine src/state.rs — would gen4 hit it? what can you say from source); ${WT}/configs/eval/fp_budget_ladder.yaml and ${WT}/configs/eval/ch3_r4_fp_anchor.yaml (the FP@20 protocol and its two standing disclosures — quote them verbatim); ${WT}/scripts/ch3_fp_h2h.py and ${WT}/scripts/ch3_r4_fp_runner.sh (what is gen1-specific in the harness); and ${PW}/pokejax/{ANALYSIS_local_heuristic_winrate.md,ANALYSIS_winrate_gap.md,local_summary.json,training_log.txt} plus the index's pokejax entry (the obs-bridge bug list: stale available_moves after switches on 15.9% of turns, PP never decrementing locally, sleep-turn off-by-one). Deliver: (1) a feasibility assessment: does poke-engine expose a gen4 feature flag; does foul-play have gen4 mechanics and set support (evidence: code paths, set files, tests); what a 'make poke_engine GEN=gen4' would require; what needs live verification (a gen4 smoke battle vs SH post-fleet) and the exact check; the Struggle / turn-1000 risk for gen4 (stall wars with Leftovers / Protect / Toxic are common in gen4 — assess qualitatively and point at the Endless Battle Clause); (2) Wang's MCTS as the alternative search anchor: cite only what the index says (another agent reads the thesis) and what infrastructure it needs; (3) pokejax's gen4 engine choices and its bug list turned into an audit checklist for OUR gen4 bridge (each item: what to check in poke-env 0.15.0, from source now or live later). Note name: foulplay_pokeengine_gen4_pokejax.md` },

  { key: 'project_record', agentType: 'doc-archaeologist', prompt: `Work from the project record ONLY, in ${WT} (identical to main @ 60c1225) plus two gitignored on-disk files in MAIN. Files: ${WT}/SESSION_LOGS.md (index it with grep -n '^- 20' then read entries by offset/limit; never a broad keyword grep), ${WT}/RESULTS.md (§16 ladder, anchor sections, disclosure lines), ${WT}/CHAPTER5.md (§3 licensed A2, §6, §7 rulings), ${WT}/CLEANUP.md, ${WT}/readouts/*.md, ${WT}/configs/eval/fp_budget_ladder.yaml, ${WT}/configs/eval/ch3_r4_fp_anchor.yaml, ${WT}/configs/showdown_sp_100m.yaml (header only), ${WT}/IDEAS_POST_100M.md §2.5 and §2.6, ${WT}/JOURNEY.md, ${WT}/HANDOFF.md, ${WT}/STATUS.md, and ${MAIN}/results/design_ch5_100m/synthesis_100m.md and BRIEF_100M.md (results/ is not runs/ or logs/, reading it is allowed). Assemble with citations (file:line, or entry date plus line): (1) everything recorded about Wang 2024: the 2026-08-03 verification entries (which claims were verified or corrected: hyperparameters, encoder, action-space size, MCTS, LR ablation, the 0.786-vs-0.85 discrepancy, the 'poke-env fixes upstreamed by 0.15.0' claim and its evidence) and every later mention; (2) the anchor-battery convention verbatim: the 2026-08-23 ruling, the MU-2 amendment of 2026-08-26, the locked vs-SH protocol, the BC-clone h2h 500 protocol (what exactly the BC clone is: which teacher, which data, how it is built — see scripts/make_bc_dataset.py and the readouts), the FP@20 protocol and its two standing disclosures verbatim, the FP budget ladder, 'match the policy form to the rating', 'anchors are descriptive never verdict inputs', the ladder-plus-Foul-Play pairing rule, and the disclosure lines that travel with every headline number (N-TIMER, G9 delta, A-COLL, N-ANNEAL); (3) the search-depreciation data points with full provenance: the 12M per-lane search deltas +0.051 / +0.104 / +0.148 (which lanes and seeds, which checkpoints, which search form and budget, n, the pooled 0.79283 vs the greedy 0.71825), the 50M-era search reads (LADDER R3's object: one-ply search@M on lane s80; RESULTS §16 rows; R4S66 search@20 0.38067 vs greedy 0.4740 off-FP and any vs-SH counterpart), the Chapter 3 close numbers, the 'search substitutes for a deficient value head' hypothesis text (JOURNEY plus the CH3 / D18 / D22 critic diagnostics and the Brier decomposition), and the 100M primary as the pending endpoint; (4) most-damage-typed: any recorded ruling or spec beyond JOURNEY and IDEAS §2.6; how MaxBasePowerPlayer is used today (the milestone ladder in rl/envs/showdown.py OPPONENT_PLAYERS; any eval config); (5) the ladder-object question (greedy vs searched; HANDOFF §4.4) and the standing maintainer rulings that touch gen4 or cross-generation transfer ('Weights never transfer between generations': ruling or JOURNEY note?), the 2026-08-23 ladder and big-runs rulings, the pre-reg conventions (the credit line verbatim, the five pre-reg rules, the journey_step header requirement, the 2-Opus cycle rule for irreversible artifacts); (6) the 2-Opus house style for adjudications: quote 2-3 adjudication entries from synthesis_100m.md so a writer can imitate the form (recommendation, losing argument, carried-to-ratification, refusal lists). Note name: project_record.md` },

  { key: 'mechanics_literature', prompt: `Build the gen1 -> gen4 rules delta from the LITERATURE side (Bulbapedia generation-mechanics pages, Smogon's DPP resources, Showdown's own docs and sim comments on GitHub), as an independent cross-check of the data-file agents. For each item give the literature claim, the URL, and the exact vendored-data location where it should be verified (${SD}/data/mods/gen4/..., ${SD}/data/conditions.ts, ${SD}/data/moves.ts, ${SD}/data/typechart.ts, ${SD}/sim/...); verify in the data file when it is cheap (a grep) and tag tree-verified when you did, literature-only when you did not. Cover: the physical/special split (gen4 per-move category; gens 1-3 by type); types 15 -> 17 (Dark and Steel added in gen2; the specific type-chart cells that changed from gen1: Ghost vs Psychic 0x -> 2x, Bug vs Poison 2x -> 0.5x, Poison vs Bug 2x -> 1x, Ice vs Fire 1x -> 0.5x; the Steel resistances; Dark's immunity to Psychic; Ghost/Dark vs Steel); held items (gen2+; which classes matter); abilities (gen3+); weather (gen2+; ability weather gen3+; gen4 ability weather lasts until replaced); entry hazards (Spikes gen2, Stealth Rock and Toxic Spikes gen4); status changes (sleep duration by generation and whether the wake turn allows a move; freeze thaw chance by generation; paralysis speed factor; burn; Toxic counter behaviour on switch; confusion; Attract); volatiles and moves new in gens 2-4 that shape play: Substitute (gen1 vs gen4 behaviour), Protect / Detect, Encore, Taunt, Trick, U-turn, Pursuit, Roar / Whirlwind, Rapid Spin, Baton Pass, Perish Song, Wish, Rest / Sleep Talk, Toxic, Will-O-Wisp, Thunder Wave, the Stealth Rock damage table, Explosion / Selfdestruct halving Defense in gens 1-4, Hidden Power; critical-hit mechanics (gen1 speed-based vs gen2+ stages; gen4 base rate 1/16; multiplier 2x; high-crit moves); accuracy / evasion; priority brackets and speed ties; PP and Pressure; stat mechanics (gen1 single Special vs SpA/SpD; natures / EVs / IVs from gen3 and what randbats does with them); team preview (introduced in gen5, so gens 1-4 have none); the Endless Battle Clause; Struggle recoil by generation; Rest turns; Roost; Trick Room; Gravity; multi-hit distributions; OHKO moves and clauses; the Sleep Clause Mod; the Freeze Clause. Also flag which gen2/gen3-era additions an encoder for gen4 must carry even though they are not gen4-new, and say so explicitly. Note name: mechanics_literature.md` },
]

phase('Sweep')
log('Sweep: ' + TASKS.length + ' source-family agents')
const sweep = await parallel(TASKS.map(t => () =>
  agent(PREAMBLE + t.prompt, { label: 'research:' + t.key, phase: 'Sweep', schema: RESEARCH_SCHEMA, agentType: t.agentType })
    .then(r => r ? { key: t.key, ...r } : null)
))
const done = sweep.filter(Boolean)
const missing = TASKS.map(t => t.key).filter(k => !done.some(d => d.key === k))
log('Sweep done: ' + done.length + '/' + TASKS.length + (missing.length ? ' MISSING: ' + missing.join(', ') : ''))

phase('Reconcile')
const CRITIC_SCHEMA = {
  type: 'object',
  required: ['index_path', 'conflicts', 'gaps', 'followups'],
  properties: {
    index_path: { type: 'string' },
    conflicts: { type: 'array', items: { type: 'object', required: ['topic', 'positions', 'settle_by'], properties: { topic: { type: 'string' }, positions: { type: 'string' }, settle_by: { type: 'string' } } } },
    gaps: { type: 'array', items: { type: 'string' } },
    followups: { type: 'array', items: { type: 'object', required: ['name', 'prompt'], properties: { name: { type: 'string' }, prompt: { type: 'string' } } } },
  },
}
const critic = await agent(PREAMBLE + `You are the RECONCILE critic, not a source agent. Read EVERY research note under ${RD} (ls ${RD}; read each .md whose name does not start with an underscore, in full). Missing agents (their notes may not exist): ${missing.length ? missing.join(', ') : 'none'}. Produce ${RD}/INDEX.md containing: (1) a one-paragraph abstract per note with its path; (2) a CONFLICTS table: every place two notes disagree on a fact (sleep turn counts, Wang hyperparameters, vocab sizes, poke-env field semantics, SH behaviour, item lists, ability semantics, anything): quote both positions with their citations, name which has the stronger verification status, and say what would settle it; (3) a GAPS list against the six deliverables (mechanics_delta, pokeenv_gen4_survey, encoder_requirements, anchors_and_eval, open_questions, search_depreciation): claims still literature-only that could be tree- or source-verified cheaply; sources named in the brief that no note actually read (check each: Wang thesis Tables A.1 / A.2 / A.3 and §3.1.4 / §5.1.3, wang_fork_diffs.md all three sections, the H&L PDF and the metagrok bot code, all four pokejax files, poke-env singles_env / env / battle / pokemon / move / baselines, all eight SD/data/mods/gen4 files, both random-battles/gen4 files and the gen5 base they inherit, the SD/config/formats.ts gen4randombattle entry, AUDIT F-08 and the audit branch tests, our rl/envs/showdown.py in full, IDEAS §2.5 / §2.6, the anchor-battery session-log entries of 2026-08-23 and 2026-08-26, ps-ppo obs files, the Metamon appendix); and gen4 mechanics with no owner; (4) up to 8 FOLLOW-UP tasks, each a self-contained agent prompt (a short name for the note file, the precise question, the exact files or pages to read, what to return) that closes one specific conflict or gap; prioritize load-bearing facts for the encoder and the anchors over completeness for its own sake. Return index_path, conflicts, gaps, followups via the schema. Your own note is INDEX.md.`, { label: 'reconcile-critic', phase: 'Reconcile', schema: CRITIC_SCHEMA })

let followups = []
if (critic && critic.followups && critic.followups.length) {
  phase('Follow-up')
  const fu = critic.followups.slice(0, 8)
  log('Follow-ups: ' + fu.map(f => f.name).join(', '))
  followups = (await parallel(fu.map(f => () =>
    agent(PREAMBLE + `FOLLOW-UP TASK named '${f.name}' from the reconcile critic (its index is at ${RD}/INDEX.md; read the notes it points at as needed):\n\n` + f.prompt + `\n\nNote name: ${f.name}.md`, { label: 'followup:' + f.name, phase: 'Follow-up', schema: RESEARCH_SCHEMA })
      .then(r => r ? { key: f.name, ...r } : null)
  ))).filter(Boolean)
  log('Follow-ups done: ' + followups.length + '/' + fu.length)
} else {
  log('No follow-ups named by the critic' + (critic ? '' : ' (critic returned null)'))
}

return { sweep: done, missing, critic, followups }