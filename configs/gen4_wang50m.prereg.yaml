# SIDECAR of configs/gen4_wang50m.yaml — the machine-readable half of the
# pre-registration (a training Config accepts no extra top-level keys).
# tests/test_gen4_prereg.py asserts every value here against the training
# file and the header text. Ratification writes `ratified_decisions`.
prereg_for: configs/gen4_wang50m.yaml
status: DRAFT, LAUNCH AUTHORIZED — 2-Opus review APPLIED 2026-09-05 late (review_1 9 MUST / 17 SHOULD; review_2 7 MUST / 16 SHOULD; all applied); agent-side launch authorized by the maintainer in chat 2026-09-06 ~02:00 UTC at the recommended defaults (see launch_authorization); RW-1/RW-4 still owed
journey_step: "3 -> 4"
credits_nothing: true
primary:
  axis: "vs SimpleHeuristicsPlayer, locked protocol: final checkpoint, 3000 battles/lane, 3 lanes, deterministic policy, ties as non-wins"
  aggregator: "EQUAL-WEIGHT MEAN of the three per-lane finals"
  instrument: "scripts/eval_checkpoint.py <ckpt> --episodes 3000"
  milestone_threshold: 0.60        # JOURNEY step 3 (M-YES iff pooled >= 0.60)
  step5_threshold: 0.756           # JOURNEY step 5 AS RULED; arithmetic 0.786 - 0.0290 = 0.757 (RW-1)
  step5_target: 0.786              # Wang Table 4.1, row NN (network ALONE; MCTS+NN vs Heuristic is 0.908) [R1-3]
  step5_target_is_his_weaker_number: "Figure 4.1 digitizes to ~0.836 endpoint / ~0.849 peak near 120M, unreconciled [R1-9]"
  wang_se: 0.0290                  # one se at n = 200 — thesis §3.1.2's VALIDATION n; Table 4.1 states no n [R1-4]
  wang_table41_implied_n: 1000     # every Table 4.1 cell fits n = 1000 (0.786*200 = 157.2 does not); se 0.0130, floor 0.773
  sidedness: one-sided, no ceiling
  k_min_for_a_branch: 3
  n_eff:                           # ruling 10 [R1-6]
    primary: "battles with an env-supplied info['outcome']; a dead eval is re-run from zero, never merged; n_eff = 3000 per lane or that lane's primary is PENDING"
    fp_legs: "seat_finished - crash_forfeits, crash_forfeits = FP relaunches (recorded, not acted on); VOID above 5 relaunches per lane"
    cross_check: "win_rate == wins_from_returns exactly on every number (nothing is shaped); mask_desyncs disclosed beside each"
wang_recipe:                       # Table A.3 + §3.1.4, verified against the thesis text 2026-09-05
  learning_rate: 5.8884e-5         # 10^-4.23
  lr_schedule: "lr0 / (8x + 1)^1.5"
  n_epochs: 7
  gamma: 0.9999
  gae_lambda: 0.754
  clip_range: 0.0829
  clip_range_vf: 0.0184
  entropy_coef: 0.0588
  value_coef: 0.4375
  max_grad_norm: 0.543
  n_steps_total_rows: 39936        # 78 x 512
  batch_size: 1024
  per_seat_dose: 75000000          # his 150M learner steps, both seats harvested
update_arithmetic:
  seat1_rows_per_update: 19968     # num_envs 8 x rollout_steps 2496
  union_rows_per_update_nominal: 39936
  minibatches: 39
  minibatch_width_nominal: 1024
  updates: 2504                    # floor(5e7 / 19,968); last update at step 49,999,872
  minibatch_slices_per_epoch: 40   # 39 full + a 0-38-row tail under minibatch_tail keep on ~38/39 of updates [R1-2, R2-M3]
  grad_steps_approx: 700000        # 2,504 x 7 x ~39.95
  rungs_per_lane: 100
  rung_mb: 14.7                    # a ladder rung (no pool member); checkpoint.pt with the member is 19.6 MB [R2-S2]
  lr_floor_factor: 27.0            # (8 + 1)^1.5 asymptotically; realized minimum lr0/26.99 at the last update [R1-10]
  d_a_form: "x = ((u - 1) * 19968) / 5e7 with u the checkpoint's `updates` field; lr == 5.8884e-5 * (8x + 1)^-1.5 to 1e-12 [R1-1]"
  d_b_fleet_expected_sps: 203      # 0.7 x 290 (solo smoke 296 harmonic, rounded down); band [173, 234] provisional [R1-7]
seeds:
  lanes: [200, 208, 216]
  spares: [224, 232, 240]
  window: 8
freeze:
  obs_dim: 1448
  priv_dim: 703
  layout: v0.1
  corpus_sha256: b72dcbc7e4b7a706967d8811f7ac3d05dd63e40cb01b624800807db1a308c1f4
  fixture_sha256: 16eb40c7d2850799d5913361f52e5fbd36f566f5000f2920469c684a1672b7b8
  showdown_commit: 59da482eabc87245eb62313593e468e81ca537d9
  sets_sha256: eacca5f3217721667dc49ab4f9832e9c8a14adf07b1eb12bad06c0679d96708c
  vocab_tables: [301, 183, 41, 102]   # species, moves, items, abilities incl. row 0
  params: {actor: 674763, critic: 543553}
battery:                           # CLAUDE.md's gen-4 list; descriptive, never verdict inputs
  L1: {leg: most_damage_typed_h2h, n_per_lane: 500}
  L2: {leg: foulplay_h2h, search_time_ms: 20, n_per_lane: 250, seat_form: samples}
  L3: {leg: foulplay_h2h, search_time_ms: 500, n_per_lane: 250, seat_form: samples}
  L4: {leg: bc_clone_h2h, n_per_lane: 500, teacher: "FP@20", status: PENDING}
  L5: {leg: vs_sh_per_lane_and_tie_rate, n_per_lane: 3000, note: "primary axis, descriptive use"}  # [R1-21]
  sanity_rows: [random, max_power, "FP@20 vs SH 226-24-0 (n=250)", "FP@500 vs SH 228-22-0 (n=250)", "gen-4 MDT vs SH via gen4_smoke.py (to measure)"]
  L3_chunking: "5 chunks x 50 battles per lane, --timeout 5400, tally = SUM of chunk W-L-T [R2-M5]"
  fp_tags: "gen4w50m_s<seed>_fp20 / gen4w50m_s<seed>_fp500_c<k>, --out results/gen4_wang50m/fp/, --seed <lane seed> [R2-M6]"
  q38_two_se_diff_at_750: 0.052    # equivalence means 'not distinguishable at +-5 points' [R1-19]
disclosures: [D-NET, D-ACT, D-ENC, D-IMPL, D-COLL, D-SH, D-TIE, D-DOSE]
fp_disclosures: ["equivalence test weakly powered", "point estimate flatters us", "pinned set file drifts +-1-2 levels on 40 species (gen 4)"]  # ride L2 AND L3 [R1-20]
barred_language:
  - "beats Wang"
  - "matches Wang"
  - "credited (of any number in this run)"
  - "any ladder projection from vs-SH, in either direction"
  - "flat (as an unqualified description of the S-SHAPE curve; the permitted forms are enumerated in the header)"
  - "plateau (same rule)"
  - "the pipeline failed (of M-NO without the ordered cause list)"
  - "our best gen-4 lane"
  - "on track for Wang's 0.786"
rulings_wanted: [RW-1, RW-2, RW-3, RW-4, RW-5, RW-6]
ratified_decisions:                # the maintainer's, in chat 2026-09-06 ~11:30 UTC: "defaults on both"
  RW-1: {ruling: "0.756 as ruled (0.786 minus one se at his n = 200), one-sided, no ceiling", when: "2026-09-06T11:30Z", by: maintainer, form: "chat: 'defaults on both'"}
  RW-4: {ruling: "FP@500 on ALL THREE lanes, chunked 5 x 50 per lane (--timeout 5400; the chunk is the resume unit)", when: "2026-09-06T11:30Z", by: maintainer, form: "chat: 'defaults on both'"}
  RW-2: {ruling: "baked in at launch — our trunk widths (see launch_authorization)", when: "2026-09-06T03:08Z"}
  RW-3: {ruling: "baked in at launch — update = TOTAL rows ~ 39,936", when: "2026-09-06T03:08Z"}
  RW-5: {ruling: "moot — the maintainer waived rule 4 for this run in chat", when: "2026-09-06T01:55Z"}
  RW-6: {ruling: "baked in at launch — minibatch tail kept (40 slices)", when: "2026-09-06T03:08Z"}
launch_authorization:              # NOT a ratification — the record of how the run started
  by: maintainer (chat, 2026-09-06 ~01:55 UTC — "you can start it yourself (breaking my rule)")
  rule_waived: CLAUDE.md rule 4 (> 5 h training is a hand-over launch), this run only
  form: the header's RECOMMENDED DEFAULTS for every RW — RW-2 (trunk widths ours), RW-3 (update = total rows ≈ 39,936) and RW-6 (minibatch tail kept, 40 slices) are baked into the run; RW-1 (threshold) and RW-4 (FP@500 scope) are readout-side and remain open; RW-5 is moot
  sequencer: clone chain -> fresh server -> R0-k2 bare suite (40-min deadline; a stall re-runs with the two documented live tests deselected, disclosed in logs/gen4_launch_seq.log) -> fresh server -> scripts/gen4_wang50m_wave.sh
  alert_rule: per-lane rung-cadence steps/s every 30 min; chat ALERT below 173 (D-B RECORD) and below 102 (D-B STOP band); nothing is killed by the watch
  attempts:
    - {n: 1, launched: "2026-09-06T03:08:42Z", sha: "8858393", ended: "2026-09-06T03:21:13Z OPS KILL by the agent", cause: "MaskDesyncCapExceeded — gen-4 pool-seat desyncs ~3e-5/step vs the gen-1 cap of 3 per 100k (7 in ~250k lane steps); lane s200 died 03:17Z, its immediate resume crashed on the still-open rooms; archived under runs/aborted_20260906_0308Z/", disclosure: "lanes reached <= ~100k steps; nothing of attempt 1 is read"}
    - {n: 2, fix: "e37a7fc (PoolPlayer re-decides once on a moved request; phantom seat-2 steps answered with a default order — no phantom harvest rows)", smoke: "60k steps, 0 desyncs, 0 re-decisions, harvest ratio 0.996-1.005, D-A PASS", launched: "2026-09-06T03:40:18Z (s200) / 03:41:18Z (s208) / 03:42:18Z (s216)", sha: "d51fa6f", preflight: "PASS (disk 166 GiB, mem 14 GB, fresh server pid 11553); R0-k2 860 passed / 17 skipped / 2 live tests deselected (DISCLOSE)", resumes: "s216 x3 (10:55Z eval-guard death; 11:00Z retry; 11:07Z rollover — all from step 5,431,296), s200 x1 (11:18Z rollover from 5,750,784), s208 x1 (11:30Z rollover from 5,910,528) — each splits the history (merge_history.py) and loses <= one rollout of seat-2 rows; the rollovers carried eval fix 07f587d; watcher v2 bc6f697 from 11:02Z"}
